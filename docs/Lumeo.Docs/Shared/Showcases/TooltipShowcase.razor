@namespace Lumeo.Docs.Shared.Showcases

@* TooltipContent is positioned via Interop.PositionFixed — per the wave brief this shows the
   trigger plus a static replica of the bubble (bg-primary, text-primary-foreground, rounded-md
   — lifted from TooltipContent) rendered beside it instead of a hover-driven open. *@
<div class="w-full max-w-[22rem]">
    <Flex Align="center" Gap="3">
        <Button Variant="Button.ButtonVariant.Outline" Size="Button.ButtonSize.Sm">Hover me</Button>
        <div class="rounded-md bg-primary px-3 py-1.5 text-xs text-primary-foreground shadow">
            Add to library
        </div>
    </Flex>
</div>
