@namespace Lumeo

<nav aria-label="breadcrumb" class="@CssClass" @attributes="AdditionalAttributes">
    @ChildContent
</nav>

@code {
    /// <summary>Typically a single <see cref="BreadcrumbList"/> containing the trail's items.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>Additional CSS classes merged onto the root <c>nav</c> element.</summary>
    [Parameter] public string? Class { get; set; }

    /// <summary>Unmatched attributes are splatted onto the root <c>nav</c> element.</summary>
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private string CssClass => Cx.Merge(Class);
}
