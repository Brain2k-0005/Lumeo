@namespace Lumeo.Docs.Shared.Showcases

@* DropdownMenuContent renders position:fixed via Interop.PositionFixed, so its open
   content escapes the 16:9 preview box's overflow-hidden the same way Menubar's does
   (see README rule 4) — no special handling needed, safe to leave closed by default. *@
<div class="w-full max-w-[22rem]">
    <DropdownButton Text="Actions">
        <MenuContent>
            <DropdownMenuItem>Edit</DropdownMenuItem>
            <DropdownMenuItem>Duplicate</DropdownMenuItem>
            <DropdownMenuSeparator />
            <DropdownMenuItem>Delete</DropdownMenuItem>
        </MenuContent>
    </DropdownButton>
</div>
