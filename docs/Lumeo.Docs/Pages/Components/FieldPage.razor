@page "/components/field"

<PageTitle>Field — Lumeo</PageTitle>

<PageHeader Title="Field" Description="Label, control, description and error as one spaced unit. shadcn's form-layout primitive, the one every login and signup block is built from." />

<InstallUsage Component="Field" />

<Alert Variant="Alert.AlertVariant.Info" Title="A host with its own Field type" ShowIcon="true" Class="mt-6">
    <code class="rounded bg-muted px-1 text-xs">Lumeo.Field</code> is a bare type name, so a project that already has a <code class="rounded bg-muted px-1 text-xs">Field</code> (a generated i18n key group, say) sees CS0104 everywhere once <code class="rounded bg-muted px-1 text-xs">@@using Lumeo</code> is in _Imports.razor. One alias settles it: <code class="rounded bg-muted px-1 text-xs">@@using Field = My.App.Field;</code> in _Imports.razor, an alias wins over a using; write <code class="rounded bg-muted px-1 text-xs">&lt;Lumeo.Field&gt;</code> where you want this component.
</Alert>

<div class="mt-10 space-y-3">
    <Heading Id="when-to-use" Level="2" Class="scroll-mt-20">When to Use</Heading>
    <ul class="list-disc list-inside space-y-1 text-muted-foreground text-sm">
        <li>Any form: a <code class="rounded bg-muted px-1 text-xs">FieldGroup</code> spaces fields 20px apart, a <code class="rounded bg-muted px-1 text-xs">Field</code> spaces its label, control and description 8px apart</li>
        <li>A checkbox or switch beside its text: <code class="rounded bg-muted px-1 text-xs">Orientation="Horizontal"</code> with the text in a <code class="rounded bg-muted px-1 text-xs">FieldContent</code></li>
        <li>A rule with a caption between two parts of a form: <code class="rounded bg-muted px-1 text-xs">FieldSeparator</code></li>
        <li>Validation messages that must read once even when two rules fire: <code class="rounded bg-muted px-1 text-xs">FieldError</code></li>
    </ul>
</div>

<Stack Gap="8" Class="mt-8">
    <ComponentDemo Title="Login form" Description="shadcn's login-01, the block as it ships." Code="@_loginCode">
        <Card Class="w-full max-w-sm">
            <CardHeader>
                <CardTitle>Login to your account</CardTitle>
                <CardDescription>Enter your email below to login to your account</CardDescription>
            </CardHeader>
            <CardContent>
                <FieldGroup>
                    <Field>
                        <FieldLabel For="field-demo-email">Email</FieldLabel>
                        <Input id="field-demo-email" Type="email" Placeholder="m@example.com" />
                    </Field>
                    <Field>
                        <div class="flex items-center">
                            <FieldLabel For="field-demo-password">Password</FieldLabel>
                            <a href="#" class="ml-auto inline-block text-sm underline-offset-4 hover:underline">Forgot your password?</a>
                        </div>
                        <Input id="field-demo-password" Type="password" />
                    </Field>
                    <Field>
                        <Button>Login</Button>
                        <Button Variant="Button.ButtonVariant.Outline">Login with Google</Button>
                        <FieldDescription Class="text-center">Don't have an account? <a href="#">Sign up</a></FieldDescription>
                    </Field>
                </FieldGroup>
            </CardContent>
        </Card>
    </ComponentDemo>

    <ComponentDemo Title="Horizontal" Description="The control beside its text. FieldContent keeps label and description in their own tight column." Code="@_horizontalCode">
        <FieldGroup Class="max-w-sm">
            <Field Orientation="Field.FieldOrientation.Horizontal">
                <Checkbox id="field-demo-terms" />
                <FieldContent>
                    <FieldLabel For="field-demo-terms">Accept terms and conditions</FieldLabel>
                    <FieldDescription>By clicking this checkbox, you agree to the terms and conditions.</FieldDescription>
                </FieldContent>
            </Field>
            <Field Orientation="Field.FieldOrientation.Horizontal">
                <FieldContent>
                    <FieldLabel For="field-demo-notify">Email notifications</FieldLabel>
                    <FieldDescription>Receive a summary every morning.</FieldDescription>
                </FieldContent>
                <Switch id="field-demo-notify" />
            </Field>
        </FieldGroup>
    </ComponentDemo>

    <ComponentDemo Title="Fieldset, separator and errors" Code="@_setCode">
        <FieldGroup Class="max-w-sm">
            <FieldSet>
                <FieldLegend>Address</FieldLegend>
                <FieldDescription>Where the order ships.</FieldDescription>
                <FieldGroup>
                    <Field>
                        <FieldLabel For="field-demo-street">Street</FieldLabel>
                        <Input id="field-demo-street" />
                    </Field>
                    <Field data-invalid="true">
                        <FieldLabel For="field-demo-city">City</FieldLabel>
                        <Input id="field-demo-city" Invalid="true" />
                        <FieldError Errors="@(new[] { "City is required", "City is required" })" />
                    </Field>
                </FieldGroup>
            </FieldSet>
            <FieldSeparator>Or continue with</FieldSeparator>
            <Field>
                <Button Variant="Button.ButtonVariant.Outline">Continue with GitHub</Button>
            </Field>
        </FieldGroup>
    </ComponentDemo>
</Stack>

<Stack Gap="8" Class="mt-12">
    <Stack Gap="3">
        <Heading Id="api-reference" Level="2" Class="scroll-mt-20">API Reference</Heading>
        <Lumeo.Text Size="sm" Color="muted">
            Every member takes <code class="rounded bg-muted px-1 text-xs">Class</code> and splats unmatched attributes onto its root. The parameters below are the ones beyond that.
        </Lumeo.Text>
        <div class="overflow-x-auto rounded-lg border border-border/60">
            <table class="w-full text-sm">
                <thead>
                    <tr class="border-b border-border/60 bg-muted/50">
                        <th class="p-3 text-left font-medium">Parameter</th>
                        <th class="p-3 text-left font-medium">Type</th>
                        <th class="p-3 text-left font-medium">Default</th>
                        <th class="p-3 text-left font-medium">Description</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td class="p-3 font-mono text-xs text-primary">Orientation</td>
                        <td class="p-3 font-mono text-xs text-muted-foreground">Field.FieldOrientation</td>
                        <td class="p-3 font-mono text-xs text-muted-foreground">Vertical</td>
                        <td class="p-3 text-muted-foreground">On Field. Vertical stacks label, control and description; Horizontal puts the control beside them; Responsive is Vertical until the enclosing FieldGroup is md-wide.</td>
                    </tr>
                    <tr>
                        <td class="p-3 font-mono text-xs text-primary">For</td>
                        <td class="p-3 font-mono text-xs text-muted-foreground">string?</td>
                        <td class="p-3 font-mono text-xs text-muted-foreground">null</td>
                        <td class="p-3 text-muted-foreground">On FieldLabel. The id of the control the label is for.</td>
                    </tr>
                    <tr>
                        <td class="p-3 font-mono text-xs text-primary">Variant</td>
                        <td class="p-3 font-mono text-xs text-muted-foreground">FieldLegend.LegendVariant</td>
                        <td class="p-3 font-mono text-xs text-muted-foreground">Legend</td>
                        <td class="p-3 text-muted-foreground">On FieldLegend. Legend is the fieldset title (16px); Label sizes it like a field label for a group of checkboxes or radios.</td>
                    </tr>
                    <tr>
                        <td class="p-3 font-mono text-xs text-primary">Errors</td>
                        <td class="p-3 font-mono text-xs text-muted-foreground">IEnumerable&lt;string?&gt;?</td>
                        <td class="p-3 font-mono text-xs text-muted-foreground">null</td>
                        <td class="p-3 text-muted-foreground">On FieldError. Messages to show when there is no child content. Blank entries are skipped, duplicates collapse, several become a list, none renders nothing.</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </Stack>
</Stack>

@code {
    private const string _loginCode = @"<Card Class=""w-full max-w-sm"">
    <CardHeader>
        <CardTitle>Login to your account</CardTitle>
        <CardDescription>Enter your email below to login to your account</CardDescription>
    </CardHeader>
    <CardContent>
        <FieldGroup>
            <Field>
                <FieldLabel For=""email"">Email</FieldLabel>
                <Input id=""email"" Type=""email"" Placeholder=""m@example.com"" />
            </Field>
            <Field>
                <div class=""flex items-center"">
                    <FieldLabel For=""password"">Password</FieldLabel>
                    <a href=""#"" class=""ml-auto inline-block text-sm underline-offset-4 hover:underline"">Forgot your password?</a>
                </div>
                <Input id=""password"" Type=""password"" />
            </Field>
            <Field>
                <Button>Login</Button>
                <Button Variant=""Button.ButtonVariant.Outline"">Login with Google</Button>
                <FieldDescription Class=""text-center"">Don't have an account? <a href=""#"">Sign up</a></FieldDescription>
            </Field>
        </FieldGroup>
    </CardContent>
</Card>";

    private const string _horizontalCode = @"<FieldGroup>
    <Field Orientation=""Field.FieldOrientation.Horizontal"">
        <Checkbox id=""terms"" />
        <FieldContent>
            <FieldLabel For=""terms"">Accept terms and conditions</FieldLabel>
            <FieldDescription>By clicking this checkbox, you agree to the terms and conditions.</FieldDescription>
        </FieldContent>
    </Field>
    <Field Orientation=""Field.FieldOrientation.Horizontal"">
        <FieldContent>
            <FieldLabel For=""notify"">Email notifications</FieldLabel>
            <FieldDescription>Receive a summary every morning.</FieldDescription>
        </FieldContent>
        <Switch id=""notify"" />
    </Field>
</FieldGroup>";

    private const string _setCode = @"<FieldGroup>
    <FieldSet>
        <FieldLegend>Address</FieldLegend>
        <FieldDescription>Where the order ships.</FieldDescription>
        <FieldGroup>
            <Field>
                <FieldLabel For=""street"">Street</FieldLabel>
                <Input id=""street"" />
            </Field>
            <Field data-invalid=""true"">
                <FieldLabel For=""city"">City</FieldLabel>
                <Input id=""city"" Invalid=""true"" />
                <FieldError Errors=""@(new[] { ""City is required"" })"" />
            </Field>
        </FieldGroup>
    </FieldSet>
    <FieldSeparator>Or continue with</FieldSeparator>
    <Field>
        <Button Variant=""Button.ButtonVariant.Outline"">Continue with GitHub</Button>
    </Field>
</FieldGroup>";
}
