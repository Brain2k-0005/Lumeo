@page "/components/code"

<PageTitle>Code — Lumeo</PageTitle>

<PageHeader Title="Code" Description="A component for displaying inline or block code snippets with consistent styling." />

<Stack Gap="8">
    <ComponentDemo Title="Inline Code" Code="@_inlineCode">
        <Text>
            Use the <Code>Console.WriteLine()</Code> method to print output. You can also use <Code>Debug.Log()</Code> for debugging.
        </Text>
    </ComponentDemo>

    <ComponentDemo Title="Block Code" Code="@_blockCode">
        <Code Variant="block" Class="w-full max-w-lg">
            var builder = WebApplication.CreateBuilder(args);<br />
            builder.Services.AddRazorComponents();<br />
            var app = builder.Build();<br />
            app.Run();
        </Code>
    </ComponentDemo>

    <ComponentDemo Title="With Custom Size" Code="@_sizeCode">
        <Stack Gap="3">
            <Text>Small: <Code Size="xs">npm install</Code></Text>
            <Text>Default: <Code>npm install</Code></Text>
            <Text>Large: <Code Size="lg">npm install</Code></Text>
        </Stack>
    </ComponentDemo>

    <!-- API Reference -->
    <div>
        <Heading Level="2" Class="mb-4">API Reference</Heading>
        <Table>
            <TableHeader>
                <TableRow>
                    <TableHead>Prop</TableHead>
                    <TableHead>Type</TableHead>
                    <TableHead>Default</TableHead>
                    <TableHead>Description</TableHead>
                </TableRow>
            </TableHeader>
            <TableBody>
                <TableRow>
                    <TableCell Class="font-mono text-xs">Variant</TableCell>
                    <TableCell Class="font-mono text-xs">string</TableCell>
                    <TableCell Class="font-mono text-xs">"inline"</TableCell>
                    <TableCell>Display variant. "inline" for within text, "block" for standalone code blocks.</TableCell>
                </TableRow>
                <TableRow>
                    <TableCell Class="font-mono text-xs">Size</TableCell>
                    <TableCell Class="font-mono text-xs">string?</TableCell>
                    <TableCell Class="font-mono text-xs">null</TableCell>
                    <TableCell>Font size override. Maps to text-{value}.</TableCell>
                </TableRow>
            </TableBody>
        </Table>
    </div>
</Stack>

@code {
    private string _inlineCode = @"<Text>
    Use the <Code>Console.WriteLine()</Code> method to print output.
</Text>";

    private string _blockCode = @"<Code Variant=""block"">
    var builder = WebApplication.CreateBuilder(args);
    builder.Services.AddRazorComponents();
    var app = builder.Build();
    app.Run();
</Code>";

    private string _sizeCode = @"<Code Size=""xs"">npm install</Code>
<Code>npm install</Code>
<Code Size=""lg"">npm install</Code>";
}
