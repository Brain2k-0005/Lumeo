@page "/components/carousel"

<PageTitle>Carousel — Lumeo</PageTitle>

<PageHeader Title="Carousel" Description="A carousel with motion and swipe built using scroll snapping." />

<div class="space-y-8">
    <ComponentDemo Title="Default" Code="@_defaultCode">
        <div class="w-full max-w-xs mx-auto">
            <Carousel>
                <CarouselContent>
                    @for (var i = 1; i <= 5; i++)
                    {
                        var index = i;
                        <CarouselItem>
                            <div class="p-1">
                                <Card>
                                    <CardContent Class="flex aspect-square items-center justify-center p-6">
                                        <span class="text-4xl font-semibold">@index</span>
                                    </CardContent>
                                </Card>
                            </div>
                        </CarouselItem>
                    }
                </CarouselContent>
                <CarouselPrevious />
                <CarouselNext />
            </Carousel>
        </div>
    </ComponentDemo>

    <ComponentDemo Title="With Loop" Code="@_loopCode">
        <div class="w-full max-w-xs mx-auto">
            <Carousel Loop="true">
                <CarouselContent>
                    @for (var i = 1; i <= 5; i++)
                    {
                        var index = i;
                        <CarouselItem>
                            <div class="p-1">
                                <Card>
                                    <CardContent Class="flex aspect-square items-center justify-center p-6">
                                        <span class="text-4xl font-semibold">@index</span>
                                    </CardContent>
                                </Card>
                            </div>
                        </CarouselItem>
                    }
                </CarouselContent>
                <CarouselPrevious />
                <CarouselNext />
            </Carousel>
        </div>
    </ComponentDemo>

    <ComponentDemo Title="Product Gallery" Code="@_galleryCode">
        <div class="w-full max-w-sm mx-auto">
            <Carousel>
                <CarouselContent>
                    @foreach (var product in _products)
                    {
                        <CarouselItem>
                            <div class="p-1">
                                <Card>
                                    <AspectRatio Ratio="4.0 / 3.0" Class="rounded-t-lg overflow-hidden bg-muted">
                                        <div class="flex items-center justify-center h-full text-muted-foreground text-sm">@product.Category</div>
                                    </AspectRatio>
                                    <CardContent Class="p-4">
                                        <p class="font-semibold text-foreground text-sm">@product.Name</p>
                                        <p class="text-sm text-muted-foreground">@product.Price</p>
                                    </CardContent>
                                </Card>
                            </div>
                        </CarouselItem>
                    }
                </CarouselContent>
                <CarouselPrevious />
                <CarouselNext />
            </Carousel>
        </div>
    </ComponentDemo>
</div>

@code {
    private string _defaultCode = @"<Carousel>
    <CarouselContent>
        @for (var i = 1; i <= 5; i++)
        {
            var index = i;
            <CarouselItem>
                <div class=""p-1"">
                    <Card>
                        <CardContent Class=""flex aspect-square items-center justify-center p-6"">
                            <span class=""text-4xl font-semibold"">@index</span>
                        </CardContent>
                    </Card>
                </div>
            </CarouselItem>
        }
    </CarouselContent>
    <CarouselPrevious />
    <CarouselNext />
</Carousel>";

    private string _loopCode = @"<Carousel Loop=""true"">
    <CarouselContent>
        @for (var i = 1; i <= 5; i++)
        {
            var index = i;
            <CarouselItem>
                <div class=""p-1"">
                    <Card>
                        <CardContent Class=""flex aspect-square items-center justify-center p-6"">
                            <span class=""text-4xl font-semibold"">@index</span>
                        </CardContent>
                    </Card>
                </div>
            </CarouselItem>
        }
    </CarouselContent>
    <CarouselPrevious />
    <CarouselNext />
</Carousel>";

    private string _galleryCode = @"<Carousel>
    <CarouselContent>
        @foreach (var product in _products)
        {
            <CarouselItem>
                <div class=""p-1"">
                    <Card>
                        <AspectRatio Ratio=""4.0 / 3.0"" Class=""rounded-t-lg overflow-hidden bg-muted"">
                            <div class=""flex items-center justify-center h-full text-sm text-muted-foreground"">@product.Category</div>
                        </AspectRatio>
                        <CardContent Class=""p-4"">
                            <p class=""font-semibold text-sm"">@product.Name</p>
                            <p class=""text-sm text-muted-foreground"">@product.Price</p>
                        </CardContent>
                    </Card>
                </div>
            </CarouselItem>
        }
    </CarouselContent>
    <CarouselPrevious />
    <CarouselNext />
</Carousel>";

    private record ProductItem(string Name, string Category, string Price);

    private readonly ProductItem[] _products =
    [
        new("Wireless Headphones", "Electronics", "$79.99"),
        new("Leather Wallet", "Accessories", "$34.99"),
        new("Running Shoes", "Footwear", "$119.99"),
        new("Coffee Mug", "Kitchen", "$14.99"),
    ];
}
