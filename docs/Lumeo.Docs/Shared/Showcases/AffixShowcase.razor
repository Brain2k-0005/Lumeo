@namespace Lumeo.Docs.Shared.Showcases
@* Static exception (see ../README.md): Affix pins content based on real window/container
   scroll, which a catalog card can't reproduce. This renders the exact visible control —
   the pinned badge — as it looks once stuck, without the scroll wiring. *@

<div class="relative w-full max-w-[22rem] h-16 rounded-lg border border-border/40 bg-muted/20 flex items-start justify-center pt-3">
    <Badge Class="px-4 py-1.5 text-sm shadow">
        Affixed — Top 0px
    </Badge>
</div>
