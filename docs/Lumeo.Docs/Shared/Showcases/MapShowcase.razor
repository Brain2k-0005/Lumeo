@namespace Lumeo.Docs.Shared.Showcases
@* Static exception (see ../README.md): Map renders through MapLibre GL against real network
   tile requests and a JS-owned canvas — nothing a catalog card can reproduce offline or
   under bUnit. This renders a theme-correct tile-pattern backdrop with the same MapInfoChip
   overlay and a marker pin, positioned exactly as the "Basic" demo's Overlay slot does. *@

<div class="relative w-full max-w-[22rem] h-40 rounded-lg border border-border/40 overflow-hidden bg-muted/40"
     style="background-image:radial-gradient(var(--color-border) 1px, transparent 1px);background-size:16px 16px;">
    <MapInfoChip Position="top-left">
        48.137, 11.575 &mdash; zoom 12
    </MapInfoChip>
    <div class="absolute inset-0 flex items-center justify-center">
        <DynamicIcon Name="MapPin" Class="h-7 w-7 text-primary drop-shadow" />
    </div>
</div>
