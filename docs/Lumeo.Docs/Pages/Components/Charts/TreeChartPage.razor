@page "/components/charts/tree"

<PageTitle>Tree Chart — Lumeo</PageTitle>

<PageHeader Title="Tree Chart" Description="Display hierarchical relationships as a tree structure. Supports horizontal (LR) and vertical (TB) orientations for org charts, file trees, and decision trees." />

<Stack Gap="8">
    <ComponentDemo Title="Organization Tree" Code="@_basicCode">
        <Container>
            <TreeChart Data="_data" Orient="LR" ShowLabels="true" />
        </Container>
    </ComponentDemo>
    <ComponentDemo Title="Loading state" Description="Shape-aware skeleton with reserved space for legend and axes — no layout shift when real data arrives.">
        <Container>
            <Stack Gap="4">
                <Stack Direction="Stack.StackDirection.Horizontal" Gap="4" Align="center" Wrap="true">
                    <Button OnClick="SimulateLoadingAsync" Disabled="@_loading">Simulate loading</Button>
                    <Stack Direction="Stack.StackDirection.Horizontal" Gap="2" Align="center">
                        <Switch @bind-Value="_keepLoading" Id="keep-loading" />
                        <label for="keep-loading" class="text-sm text-muted-foreground cursor-pointer">Hold skeleton open</label>
                    </Stack>
                </Stack>
                <TreeChart Data="_data" Orient="LR" ShowLabels="true" IsLoading="@LoadingComputed" Height="320px" />
            </Stack>
        </Container>
    </ComponentDemo>
</Stack>

<!-- API Reference -->
<Stack Gap="6" Class="mt-12">
    <Heading Level="2">API Reference</Heading>
    <Stack Gap="8">
        <Stack>
            <Heading Level="3" Class="mb-3">TreeChart</Heading>
            <Stack Class="rounded-lg border border-border overflow-x-auto">
                <table class="w-full min-w-[600px] text-sm">
                    <thead>
                        <tr class="border-b bg-muted/40">
                            <th class="text-left p-3 font-medium text-foreground">Property</th>
                            <th class="text-left p-3 font-medium text-foreground">Type</th>
                            <th class="text-left p-3 font-medium text-foreground">Default</th>
                            <th class="text-left p-3 font-medium text-foreground">Description</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-border/40">
                        <tr>
                            <td class="p-3 font-mono text-xs text-primary">Data</td>
                            <td class="p-3 font-mono text-xs text-muted-foreground">TreeData?</td>
                            <td class="p-3 font-mono text-xs text-muted-foreground">null</td>
                            <td class="p-3 text-muted-foreground">Root node with Name and optional Children.</td>
                        </tr>
                        <tr>
                            <td class="p-3 font-mono text-xs text-primary">Orient</td>
                            <td class="p-3 font-mono text-xs text-muted-foreground">string</td>
                            <td class="p-3 font-mono text-xs text-muted-foreground">"LR"</td>
                            <td class="p-3 text-muted-foreground">Tree orientation: "LR", "RL", "TB", or "BT".</td>
                        </tr>
                        <tr>
                            <td class="p-3 font-mono text-xs text-primary">ShowLabels</td>
                            <td class="p-3 font-mono text-xs text-muted-foreground">bool</td>
                            <td class="p-3 font-mono text-xs text-muted-foreground">true</td>
                            <td class="p-3 text-muted-foreground">Shows node name labels.</td>
                        </tr>
                    </tbody>
                </table>
            </Stack>
        </Stack>
    </Stack>
</Stack>

@code {
    private TreeChart.TreeData _data = new()
    {
        Name = "CEO",
        Children = new()
        {
            new() { Name = "CTO", Children = new()
            {
                new() { Name = "Engineering VP", Children = new()
                {
                    new() { Name = "Frontend Lead" },
                    new() { Name = "Backend Lead"  }
                }},
                new() { Name = "DevOps Lead" }
            }},
            new() { Name = "CMO", Children = new()
            {
                new() { Name = "Product Marketing" },
                new() { Name = "Growth"            }
            }},
            new() { Name = "CFO" }
        }
    };

    private string _basicCode = @"<TreeChart Data=""_root"" Orient=""LR"" ShowLabels=""true"" />";

    private bool _loading;
    private bool _keepLoading;
    private bool LoadingComputed => _loading || _keepLoading;

    private async Task SimulateLoadingAsync()
    {
        _loading = true;
        await Task.Delay(2000);
        _loading = false;
    }
}
