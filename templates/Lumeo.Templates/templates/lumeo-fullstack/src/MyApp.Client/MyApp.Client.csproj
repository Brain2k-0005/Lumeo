<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Keep the component namespace short (MyApp.*) even though the assembly is
         MyApp.Client, so the shared code reads cleanly. -->
    <RootNamespace>MyApp</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" Version="10.0.6" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.DevServer" Version="10.0.6" PrivateAssets="all" />
    <!-- Blazor authorization primitives: AuthenticationStateProvider, AuthorizeView,
         AuthorizeRouteView, [Authorize]. The provider here talks to the real Identity API. -->
    <PackageReference Include="Microsoft.AspNetCore.Components.Authorization" Version="10.0.6" />

    <!-- Lumeo component library (prebuilt CSS/JS ship inside the package). -->
    <!-- Points at the last PUBLISHED release, not at Directory.Build.props. A template
         pin can only name a version that exists on nuget.org: the CLI end-to-end test
         scaffolds from here and restores against the real feed, so bumping this in the
         release commit fails the build with NU1102 until the package is actually out.
         Bump $(LumeoTemplatePackageVersion) in /Directory.Build.props in the first
         commit after publishing; the pack target stamps it in here. -->
    <PackageReference Include="Lumeo" Version="5.2.0" />
    <PackageReference Include="Lumeo.DataGrid" Version="5.2.0" />
  </ItemGroup>

</Project>
