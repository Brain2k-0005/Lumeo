User-agent: *
Allow: /

# E2E test-only harness pages and the SPA's not-found route — not real content,
# never linked in navigation, never indexed. Excluded from the sitemap too, with
# a narrow exception on preview deploys only: two stakeholder-facing WIP preview
# pages (/e2e/charts-native, /e2e/gantt-v3-preview) are listed there so Cloudflare
# actually serves them by URL. This Disallow still blocks any crawler that
# respects robots.txt from indexing them regardless — a disallowed URL appearing
# in a sitemap is not crawled/indexed. Those two pages also carry their own
# <meta name="robots" content="noindex"> as a second, page-level guard.
Disallow: /e2e/
Disallow: /not-found

# Deep-link routes that Blazor WASM serves from SPA fallback — explicitly
# listing them in the sitemap so crawlers don't rely on discovery from /.
Sitemap: https://lumeo.nativ.sh/sitemap.xml
