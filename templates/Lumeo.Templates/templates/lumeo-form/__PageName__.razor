@page "/__PageRoute__"

<PageTitle>__ModelName__</PageTitle>

<Stack Gap="6">
    <Heading Level="1">Contact us</Heading>
    @__ModelName__Model.RenderForm(_model, _onSubmit)
</Stack>

@code {
    private __ModelName__Model _model = new();

    private EventCallback<__ModelName__Model> _onSubmit =>
        EventCallback.Factory.Create<__ModelName__Model>(this, OnValidSubmit);

    private Task OnValidSubmit(__ModelName__Model model)
    {
        // TODO: handle submit
        return Task.CompletedTask;
    }
}
