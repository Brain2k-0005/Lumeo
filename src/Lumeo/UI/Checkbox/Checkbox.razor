@namespace Lumeo

@if (!string.IsNullOrEmpty(Label))
{
    <div class="flex items-start gap-3">
        <button type="button" id="@_id" role="checkbox" aria-checked="@AriaChecked" class="@CssClass" @onclick="Toggle" disabled="@Disabled" @attributes="AdditionalAttributes">
            @if (IsIndeterminate)
            {
                <Blazicon Svg="Lucide.Minus" class="h-3.5 w-3.5 text-current" />
            }
            else if (Checked)
            {
                <Blazicon Svg="Lucide.Check" class="h-3.5 w-3.5 text-current" />
            }
        </button>
        <div class="grid gap-1.5 leading-none">
            <label for="@_id" class="text-sm font-medium leading-none cursor-pointer peer-disabled:cursor-not-allowed peer-disabled:opacity-70">@Label</label>
            @if (!string.IsNullOrEmpty(Description))
            {
                <p class="text-xs text-muted-foreground">@Description</p>
            }
        </div>
    </div>
}
else
{
    <button type="button" id="@_id" role="checkbox" aria-checked="@AriaChecked" class="@CssClass" @onclick="Toggle" disabled="@Disabled" @attributes="AdditionalAttributes">
        @if (IsIndeterminate)
        {
            <Blazicon Svg="Lucide.Minus" class="h-3.5 w-3.5 text-current" />
        }
        else if (Checked)
        {
            <Blazicon Svg="Lucide.Check" class="h-3.5 w-3.5 text-current" />
        }
    </button>
}

@code {
    [Parameter] public bool Checked { get; set; }
    [Parameter] public EventCallback<bool> CheckedChanged { get; set; }
    [Parameter] public bool IsIndeterminate { get; set; }
    [Parameter] public EventCallback<bool> IsIndeterminateChanged { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public string? Label { get; set; }
    [Parameter] public string? Description { get; set; }
    [Parameter] public string? Id { get; set; }

    /// <summary>
    /// When true, the checkmark draws itself in via stroke-dasharray and the
    /// box plays a subtle 200ms scale bounce on check. Default is <c>false</c>.
    /// </summary>
    [Parameter] public bool Animated { get; set; }

    [Parameter] public string? Class { get; set; }
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private string _id = default!;

    protected override void OnInitialized()
    {
        _id = Id ?? $"checkbox-{Guid.NewGuid():N}";
    }

    private string AriaChecked => IsIndeterminate ? "mixed" : Checked.ToString().ToLower();

    private string CssClass => string.Join(" ", new[] { "peer h-4 w-4 shrink-0 cursor-pointer rounded-sm border border-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50", Checked || IsIndeterminate ? "bg-primary text-primary-foreground" : "bg-background", Animated && Checked ? "lumeo-checkbox-animated" : null, Class }.Where(s => !string.IsNullOrWhiteSpace(s)));

    private async Task Toggle()
    {
        if (!Disabled)
        {
            if (IsIndeterminate)
            {
                IsIndeterminate = false;
                await IsIndeterminateChanged.InvokeAsync(false);
                Checked = true;
                await CheckedChanged.InvokeAsync(true);
            }
            else
            {
                Checked = !Checked;
                await CheckedChanged.InvokeAsync(Checked);
            }
        }
    }
}
