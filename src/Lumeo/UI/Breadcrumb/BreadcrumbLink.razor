@namespace Lumeo

<a href="@Href" class="@CssClass" @attributes="AdditionalAttributes">
    @ChildContent
</a>

@code {
    /// <summary>The link's label content.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>The navigation target. Defaults to <c>"#"</c> when unset.</summary>
    [Parameter] public string? Href { get; set; } = "#";

    /// <summary>Additional CSS classes merged onto the root <c>a</c> element.</summary>
    [Parameter] public string? Class { get; set; }

    /// <summary>Unmatched attributes are splatted onto the root <c>a</c> element.</summary>
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private string CssClass => Cx.Merge("cursor-pointer transition-colors hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 rounded-sm", Class);
}
