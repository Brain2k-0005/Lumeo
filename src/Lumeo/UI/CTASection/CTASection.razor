@namespace Lumeo

@*
    CTASection — call-to-action band. Part of the 3.4 Splash Kit. Renders
    a tinted, centered band with title, optional subtitle, and an Actions
    slot for primary / secondary buttons.
*@

<section class="@CssClass" aria-labelledby="@TitleId" @attributes="AdditionalAttributes">
    <div class="mx-auto max-w-4xl px-6 py-16 sm:py-20 text-center">
        @if (!string.IsNullOrWhiteSpace(Title))
        {
            <h2 id="@TitleId" class="text-3xl sm:text-4xl font-bold tracking-tight text-foreground">@Title</h2>
        }
        @TitleSlot
        @if (!string.IsNullOrWhiteSpace(Subtitle))
        {
            <p class="mt-4 text-lg text-muted-foreground max-w-2xl mx-auto">@Subtitle</p>
        }
        @if (Actions is not null)
        {
            <div class="mt-8 flex flex-wrap items-center justify-center gap-3">
                @Actions
            </div>
        }
    </div>
</section>

@code {
    /// <summary>The band's plain-text heading, rendered as an <c>h2</c> and used to name the section landmark. Ignored when <see cref="TitleSlot"/> is set.</summary>
    [Parameter] public string? Title { get; set; }

    /// <summary>Custom heading markup, rendered instead of/in addition to <see cref="Title"/> for callers that need rich heading content.</summary>
    [Parameter] public RenderFragment? TitleSlot { get; set; }

    /// <summary>Supporting text rendered below the title.</summary>
    [Parameter] public string? Subtitle { get; set; }

    /// <summary>The call-to-action buttons, centered and wrapped below the title/subtitle.</summary>
    [Parameter] public RenderFragment? Actions { get; set; }

    /// <summary>Additional CSS classes merged onto the root <c>section</c> element.</summary>
    [Parameter] public string? Class { get; set; }

    /// <summary>Unmatched attributes splatted onto the root <c>section</c> element.</summary>
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private string CssClass => Cx.Merge("w-full bg-muted/40 border-y border-border/40", Class);

    // Name the <section> landmark via its heading so AT exposes it as a region
    // with an accessible name (#298). Null when there's no Title — a TitleSlot-
    // only consumer can still supply aria-label/aria-labelledby via attributes.
    private readonly string _titleId = $"cta-title-{Guid.NewGuid():N}";
    private string? TitleId => string.IsNullOrWhiteSpace(Title) ? null : _titleId;
}
