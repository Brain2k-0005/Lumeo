{
  "name": "Gantt",
  "category": "Data Display",
  "subcategory": "Lists & Trees",
  "description": "Gantt component.",
  "thumbnail": "/preview-cards/gantt.webp",
  "hasDocsPage": true,
  "nugetPackage": "Lumeo.Gantt",
  "files": [
    "UI/Gantt/Gantt.razor",
    "UI/Gantt/GanttTypes.cs"
  ],
  "dependencies": [
    "toggle-group"
  ],
  "packageDependencies": [],
  "cssVars": [
    "--color-border",
    "--color-card",
    "--color-destructive",
    "--radius"
  ],
  "gotchas": [],
  "api": {
    "name": "Gantt",
    "category": "Data Display",
    "subcategory": "Lists & Trees",
    "description": "Gantt component.",
    "nugetPackage": "Lumeo.Gantt",
    "files": [
      "UI/Gantt/Gantt.razor",
      "UI/Gantt/GanttTypes.cs"
    ],
    "namespace": "Lumeo",
    "inheritsFrom": null,
    "implements": [
      "IAsyncDisposable"
    ],
    "parameters": [
      {
        "name": "Tasks",
        "type": "IEnumerable<GanttTask>?",
        "default": null,
        "description": "The tasks (and milestones) rendered as bars. Two-way bindable via TasksChanged, which also fires after an in-chart drag/resize/progress edit so the parent can persist it.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "TasksChanged",
        "type": "EventCallback<IEnumerable<GanttTask>>",
        "default": null,
        "description": "Raised whenever the task set changes — either because the parent's Tasks genuinely changed, or because the user dragged a bar / changed its progress in the chart.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "ViewMode",
        "type": "GanttViewMode",
        "default": "GanttViewMode.Day",
        "description": "The active zoom level (Day/Week/Month/Year, plus the non-toolbar QuarterDay/HalfDay). Two-way bindable via ViewModeChanged; without a binding the component still tracks toolbar selections on its own. Default Day.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "ViewModeChanged",
        "type": "EventCallback<GanttViewMode>",
        "default": null,
        "description": "Raised when the user picks a new zoom level from the toolbar. Bind it (@bind-ViewMode) to keep the parent's ViewMode in lock-step with the toolbar; without a binding the component still tracks the toolbar selection on its own (uncontrolled usage).",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "ZoomLevels",
        "type": "IReadOnlyList<GanttViewMode>?",
        "default": null,
        "description": "Restricts which zoom buttons the toolbar renders, in the given order. When null or empty the full default set (Day, Week, Month, Year) is shown — matching the previous hardcoded toolbar. Only toolbar-representable levels are drawn: QuarterDay / HalfDay have no toolbar button and are silently skipped here (still settable via ViewMode). Example: ZoomLevels=\"new[]{ GanttViewMode.Day, GanttViewMode.Week, GanttViewMode.Month }\".",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "DefaultZoom",
        "type": "GanttViewMode?",
        "default": null,
        "description": "The zoom level selected on first render. When set it seeds the initial toolbar selection (and the level the timeline mounts at), overriding ViewMode's default — handy for uncontrolled usage, e.g. ZoomLevels=\"…\" DefaultZoom=\"GanttViewMode.Week\". After mount, ViewMode / @bind-ViewMode govern as before. Null falls back to ViewMode (the existing behaviour).",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "OnTaskClick",
        "type": "EventCallback<GanttTask>",
        "default": null,
        "description": "Raised when a task bar is clicked, with the clicked task.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "OnDateChange",
        "type": "EventCallback<GanttTask>",
        "default": null,
        "description": "Raised after a task bar's start/end dates change via drag, with the updated task. Also folded into TasksChanged.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "OnProgressChange",
        "type": "EventCallback<GanttTask>",
        "default": null,
        "description": "Raised after a task bar's progress changes via drag, with the updated task. Also folded into TasksChanged.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "Height",
        "type": "string",
        "default": "\"500px\"",
        "description": "CSS max-height of the chart's scrollable viewport (a CSS length string, e.g. \"500px\"). The chart shrinks to its content and only scrolls when the task list exceeds this height. Default \"500px\".",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "Readonly",
        "type": "bool",
        "default": "false",
        "description": "Disables drag-to-reschedule and drag-to-progress editing in the underlying chart.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "TodayHighlight",
        "type": "bool",
        "default": "true",
        "description": "Highlight today with a vertical line and dot marker. Default true.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "BarHeight",
        "type": "int?",
        "default": null,
        "description": "Override the pixel height of each task bar (default 22).",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "ColumnWidth",
        "type": "int?",
        "default": null,
        "description": "Override the pixel width of each date column for the current view mode.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "BarColor",
        "type": "Func<GanttTask, string?>?",
        "default": null,
        "description": "Per-task bar colour override. Return a CSS colour string (hex, rgb, or a CSS variable reference like \"var(--color-destructive)\") or null to use the primary theme colour.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "GroupBy",
        "type": "Func<GanttTask, string>?",
        "default": null,
        "description": "Optional grouping function. When set, tasks are sorted by group label and a visual section-header row is injected before the first task in each group.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "TrailingContent",
        "type": "RenderFragment?",
        "default": null,
        "description": "Extra content rendered at the end of the toolbar row, alongside the zoom-level buttons.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "OnInitError",
        "type": "EventCallback<string>",
        "default": null,
        "description": "Raised when the underlying JS chart fails to initialize, with the error message. The component also shows a destructive-styled error box in place of the chart and retries init on the next relevant parameter change.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "Class",
        "type": "string?",
        "default": null,
        "description": "Additional CSS classes merged onto the root container.",
        "isCascading": false,
        "captureUnmatched": false,
        "isEditorRequired": false
      },
      {
        "name": "AdditionalAttributes",
        "type": "Dictionary<string, object>?",
        "default": null,
        "description": "Captures any unmatched attributes and applies them to the root container.",
        "isCascading": false,
        "captureUnmatched": true,
        "isEditorRequired": false
      }
    ],
    "events": [
      {
        "name": "TasksChanged",
        "type": "EventCallback<IEnumerable<GanttTask>>",
        "description": "Raised whenever the task set changes — either because the parent's Tasks genuinely changed, or because the user dragged a bar / changed its progress in the chart."
      },
      {
        "name": "ViewModeChanged",
        "type": "EventCallback<GanttViewMode>",
        "description": "Raised when the user picks a new zoom level from the toolbar. Bind it (@bind-ViewMode) to keep the parent's ViewMode in lock-step with the toolbar; without a binding the component still tracks the toolbar selection on its own (uncontrolled usage)."
      },
      {
        "name": "OnTaskClick",
        "type": "EventCallback<GanttTask>",
        "description": "Raised when a task bar is clicked, with the clicked task."
      },
      {
        "name": "OnDateChange",
        "type": "EventCallback<GanttTask>",
        "description": "Raised after a task bar's start/end dates change via drag, with the updated task. Also folded into TasksChanged."
      },
      {
        "name": "OnProgressChange",
        "type": "EventCallback<GanttTask>",
        "description": "Raised after a task bar's progress changes via drag, with the updated task. Also folded into TasksChanged."
      },
      {
        "name": "OnInitError",
        "type": "EventCallback<string>",
        "description": "Raised when the underlying JS chart fails to initialize, with the error message. The component also shows a destructive-styled error box in place of the chart and retries init on the next relevant parameter change."
      }
    ],
    "enums": [
      {
        "name": "GanttViewMode",
        "values": [
          "QuarterDay",
          "HalfDay",
          "Day",
          "Week",
          "Month",
          "Year"
        ],
        "description": "Gantt timeline zoom level. Supported by the Lumeo SVG engine: QuarterDay = 6-hour columns, HalfDay = 12-hour columns, Day = 1 column/day, Week = 1 column/week, Month = 1 column/month, Year = 1 column/year."
      }
    ],
    "records": [],
    "gotchas": [],
    "cssVars": [
      "--color-border",
      "--color-card",
      "--color-destructive",
      "--radius"
    ],
    "examples": [
      {
        "title": "Basic project plan",
        "code": "<Gantt Tasks=\"_tasks\" ViewMode=\"GanttViewMode.Day\" Height=\"420px\" />\n\n@code {\n    private List<GanttTask> _tasks = new() {\n        new(\"research\",    \"Research\",    DateTime.Today,              DateTime.Today.AddDays(5)),\n        new(\"design\",      \"Design\",      DateTime.Today.AddDays(5),   DateTime.Today.AddDays(12), Dependencies: new[] { \"research\" }),\n        new(\"development\", \"Development\", DateTime.Today.AddDays(12),  DateTime.Today.AddDays(25), Dependencies: new[] { \"design\" }),\n        new(\"testing\",     \"Testing\",     DateTime.Today.AddDays(22),  DateTime.Today.AddDays(30), Dependencies: new[] { \"development\" }),\n        new(\"launch\",      \"Launch\",      DateTime.Today.AddDays(30),  DateTime.Today.AddDays(32), Dependencies: new[] { \"testing\" }),\n    };\n}"
      },
      {
        "title": "With progress bars",
        "code": "<Gantt Tasks=\"_tasks\" ViewMode=\"GanttViewMode.Day\" Height=\"420px\" />\n\n@code {\n    private List<GanttTask> _tasks = new() {\n        new(\"research\",    \"Research\",    DateTime.Today,             DateTime.Today.AddDays(5),  Progress: 100),\n        new(\"design\",      \"Design\",      DateTime.Today.AddDays(5),  DateTime.Today.AddDays(12), Progress: 60,  Dependencies: new[] { \"research\" }),\n        new(\"development\", \"Development\", DateTime.Today.AddDays(12), DateTime.Today.AddDays(25), Progress: 30,  Dependencies: new[] { \"design\" }),\n        // ...\n    };\n}"
      },
      {
        "title": "Drag to reschedule + change progress",
        "code": "<Gantt @bind-Tasks=\"_tasks\"\n       ViewMode=\"GanttViewMode.Day\"\n       OnDateChange=\"HandleDateChange\"\n       OnProgressChange=\"HandleProgressChange\" />\n\n@code {\n    private IEnumerable<GanttTask> _tasks = new List<GanttTask> { /* ... */ };\n    private GanttTask? _last;\n\n    Task HandleDateChange(GanttTask t)     { _last = t; StateHasChanged(); return Task.CompletedTask; }\n    Task HandleProgressChange(GanttTask t) { _last = t; StateHasChanged(); return Task.CompletedTask; }\n}"
      },
      {
        "title": "View modes",
        "code": "<Gantt Tasks=\"_tasks\" ViewMode=\"GanttViewMode.Week\" Height=\"420px\" />"
      }
    ],
    "subComponents": {},
    "a11y": {
      "roles": [],
      "ariaAttributes": [],
      "keys": [],
      "keyboardInteractive": false,
      "focusManaged": false
    },
    "parseFailed": false,
    "parseError": null
  },
  "testCoverage": {
    "tier": 3,
    "files": 11,
    "tests": 47,
    "relatedFiles": 2,
    "render": true,
    "behavior": true,
    "a11y": true,
    "keyboard": true,
    "scale": false,
    "e2e": false,
    "e2eFiles": [],
    "contract": "smoke"
  },
  "$schema": "https://lumeo.nativ.sh/registry-component-schema.json",
  "key": "gantt",
  "docsUrl": "https://lumeo.nativ.sh/components/gantt",
  "sourceUrl": [
    {
      "file": "UI/Gantt/Gantt.razor",
      "url": "https://github.com/Brain2k-0005/Lumeo/tree/master/src/Lumeo.Gantt/UI/Gantt/Gantt.razor"
    },
    {
      "file": "UI/Gantt/GanttTypes.cs",
      "url": "https://github.com/Brain2k-0005/Lumeo/tree/master/src/Lumeo.Gantt/UI/Gantt/GanttTypes.cs"
    }
  ],
  "source": [
    {
      "path": "UI/Gantt/Gantt.razor",
      "content": "@namespace Lumeo\n@implements IAsyncDisposable\n@inject Lumeo.Services.IComponentInteropService Interop\n@inject Lumeo.Services.Localization.ILumeoLocalizer L\n\n@if (_initError is not null)\n{\n    <div class=\"@($\"flex items-center justify-center text-sm text-destructive border border-destructive/30 rounded-md bg-destructive/5 {Class}\".Trim())\" @attributes=\"AdditionalAttributes\">\n        <span>@_initError</span>\n    </div>\n}\nelse\n{\n    <div class=\"@($\"flex flex-col gap-3 rounded-md border border-border/60 bg-card p-3 min-w-0 w-full {Class}\".Trim())\" @attributes=\"AdditionalAttributes\" style=\"min-width: 0; width: 100%;\">\n        @* Lumeo-styled toolbar above the Frappe Gantt canvas. We suppress\n           Quarter Day / Half Day from the control bar to keep the common case\n           simple — power users can still pass them through the `ViewMode` param. *@\n        <div class=\"flex flex-wrap items-center justify-between gap-2\">\n            <ToggleGroup Type=\"ToggleGroup.ToggleGroupType.Single\"\n                         Variant=\"ToggleGroup.ToggleGroupVariant.Outline\"\n                         Size=\"Lumeo.Size.Sm\"\n                         Value=\"@_toolbarViewValue\"\n                         ValueChanged=\"OnViewModeChangedAsync\">\n                @foreach (var level in ResolvedZoomLevels)\n                {\n                    <ToggleGroupItem Value=\"@level.ToString()\">@L[ZoomLabelKey(level)]</ToggleGroupItem>\n                }\n            </ToggleGroup>\n            @if (TrailingContent is not null)\n            {\n                <div class=\"flex items-center gap-2\">\n                    @TrailingContent\n                </div>\n            }\n        </div>\n\n        @* Grid wrapper with minmax(0, 1fr) — well-known trick to defeat the\n           min-content cascade without touching containment. The 1fr column\n           has min size 0, so the SVG inside cannot push the wrapper wider\n           than its parent.\n           Height: use max-height (cap) instead of fixed height so the chart\n           shrinks to fit its content (header + N tasks * row height) and\n           only scrolls vertically when the task list exceeds @Height. *@\n        <div style=\"display: grid; grid-template-columns: minmax(0, 1fr); width: 100%;\">\n            <div @ref=\"_hostRef\" class=\"lumeo-gantt-host\" style=\"overflow-x: auto; overflow-y: auto; max-height: @Height; min-width: 0;\"></div>\n        </div>\n    </div>\n}\n\n@code {\n    /// <summary>The tasks (and milestones) rendered as bars. Two-way bindable via <see cref=\"TasksChanged\"/>, which also fires after an in-chart drag/resize/progress edit so the parent can persist it.</summary>\n    [Parameter] public IEnumerable<GanttTask>? Tasks { get; set; }\n    /// <summary>Raised whenever the task set changes — either because the parent's <see cref=\"Tasks\"/> genuinely changed, or because the user dragged a bar / changed its progress in the chart.</summary>\n    [Parameter] public EventCallback<IEnumerable<GanttTask>> TasksChanged { get; set; }\n    /// <summary>The active zoom level (Day/Week/Month/Year, plus the non-toolbar QuarterDay/HalfDay). Two-way bindable via <see cref=\"ViewModeChanged\"/>; without a binding the component still tracks toolbar selections on its own. Default <see cref=\"GanttViewMode.Day\"/>.</summary>\n    [Parameter] public GanttViewMode ViewMode { get; set; } = GanttViewMode.Day;\n    /// <summary>\n    /// Raised when the user picks a new zoom level from the toolbar. Bind it\n    /// (<c>@bind-ViewMode</c>) to keep the parent's <see cref=\"ViewMode\"/> in\n    /// lock-step with the toolbar; without a binding the component still tracks\n    /// the toolbar selection on its own (uncontrolled usage).\n    /// </summary>\n    [Parameter] public EventCallback<GanttViewMode> ViewModeChanged { get; set; }\n    /// <summary>\n    /// Restricts which zoom buttons the toolbar renders, in the given order. When null\n    /// or empty the full default set (<c>Day, Week, Month, Year</c>) is shown — matching\n    /// the previous hardcoded toolbar. Only toolbar-representable levels are drawn:\n    /// <see cref=\"GanttViewMode.QuarterDay\"/> / <see cref=\"GanttViewMode.HalfDay\"/> have\n    /// no toolbar button and are silently skipped here (still settable via\n    /// <see cref=\"ViewMode\"/>). Example: <c>ZoomLevels=\"new[]{ GanttViewMode.Day,\n    /// GanttViewMode.Week, GanttViewMode.Month }\"</c>.\n    /// </summary>\n    [Parameter] public IReadOnlyList<GanttViewMode>? ZoomLevels { get; set; }\n    /// <summary>\n    /// The zoom level selected on first render. When set it seeds the initial toolbar\n    /// selection (and the level the timeline mounts at), overriding <see cref=\"ViewMode\"/>'s\n    /// default — handy for uncontrolled usage, e.g. <c>ZoomLevels=\"…\" DefaultZoom=\"GanttViewMode.Week\"</c>.\n    /// After mount, <see cref=\"ViewMode\"/> / <c>@bind-ViewMode</c> govern as before. Null\n    /// falls back to <see cref=\"ViewMode\"/> (the existing behaviour).\n    /// </summary>\n    [Parameter] public GanttViewMode? DefaultZoom { get; set; }\n    /// <summary>Raised when a task bar is clicked, with the clicked task.</summary>\n    [Parameter] public EventCallback<GanttTask> OnTaskClick { get; set; }\n    /// <summary>Raised after a task bar's start/end dates change via drag, with the updated task. Also folded into <see cref=\"TasksChanged\"/>.</summary>\n    [Parameter] public EventCallback<GanttTask> OnDateChange { get; set; }\n    /// <summary>Raised after a task bar's progress changes via drag, with the updated task. Also folded into <see cref=\"TasksChanged\"/>.</summary>\n    [Parameter] public EventCallback<GanttTask> OnProgressChange { get; set; }\n    /// <summary>CSS max-height of the chart's scrollable viewport (a CSS length string, e.g. <c>\"500px\"</c>). The chart shrinks to its content and only scrolls when the task list exceeds this height. Default <c>\"500px\"</c>.</summary>\n    [Parameter] public string Height { get; set; } = \"500px\";\n    /// <summary>Disables drag-to-reschedule and drag-to-progress editing in the underlying chart.</summary>\n    [Parameter] public bool Readonly { get; set; }\n    /// <summary>Highlight today with a vertical line and dot marker. Default true.</summary>\n    [Parameter] public bool TodayHighlight { get; set; } = true;\n    /// <summary>Override the pixel height of each task bar (default 22).</summary>\n    [Parameter] public int? BarHeight { get; set; }\n    /// <summary>Override the pixel width of each date column for the current view mode.</summary>\n    [Parameter] public int? ColumnWidth { get; set; }\n    /// <summary>\n    /// Per-task bar colour override. Return a CSS colour string (hex, rgb, or a\n    /// CSS variable reference like \"var(--color-destructive)\") or null to use the\n    /// primary theme colour.\n    /// </summary>\n    [Parameter] public Func<GanttTask, string?>? BarColor { get; set; }\n    /// <summary>\n    /// Optional grouping function. When set, tasks are sorted by group label and\n    /// a visual section-header row is injected before the first task in each group.\n    /// </summary>\n    [Parameter] public Func<GanttTask, string>? GroupBy { get; set; }\n    /// <summary>Extra content rendered at the end of the toolbar row, alongside the zoom-level buttons.</summary>\n    [Parameter] public RenderFragment? TrailingContent { get; set; }\n    /// <summary>Raised when the underlying JS chart fails to initialize, with the error message. The component also shows a destructive-styled error box in place of the chart and retries init on the next relevant parameter change.</summary>\n    [Parameter] public EventCallback<string> OnInitError { get; set; }\n    /// <summary>Additional CSS classes merged onto the root container.</summary>\n    [Parameter] public string? Class { get; set; }\n    /// <summary>Captures any unmatched attributes and applies them to the root container.</summary>\n    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }\n\n    private ElementReference _hostRef;\n    private string? _initError;\n    private DotNetObjectReference<Gantt>? _selfRef;\n    private string? _instanceId;\n    private bool _initialized;\n    // Guards against a second GanttInitAsync being launched while the first is\n    // still in flight. Because init is no longer latched to firstRender (it can be\n    // re-attempted after a recoverable failure), an extra render that lands during\n    // the async init handshake would otherwise re-enter and fire a duplicate\n    // gantt.init. Set true before the await, cleared in finally.\n    private bool _initializing;\n    private int _lastTasksHash;\n    // Hash of the task payload the PARENT last supplied through the `Tasks`\n    // parameter (folding in the BarColor / GroupBy delegate output, which are\n    // also parent-owned). Unlike _lastTasksHash, this is advanced ONLY by a\n    // genuine parameter change — never by a local JS drag/progress edit via\n    // ReplaceTask. In UNCONTROLLED usage (no TasksChanged binding) OnParametersSetAsync\n    // re-pushes to JS only when THIS moves, so a stale parent re-render after an\n    // uncontrolled drag no longer snaps the bars back to their pre-drag positions\n    // (battle-test wave 1, finding #18).\n    private int _lastParentHash;\n    // CONTROLLED discriminator: the hash of the local _tasks snapshot we last pushed UP\n    // via TasksChanged (also re-synced whenever we adopt an authoritative Tasks payload).\n    // A controlled parent re-render that echoes back this exact hash is safe to ignore\n    // (keep the in-flight drag/progress edit); any other hash — including one equal to\n    // the PRE-drag parent hash — is an authoritative assertion (veto / normalization /\n    // reset) and must win, rolling the local _tasks back to it.\n    private int _lastPushedTasksHash;\n    private GanttViewMode _currentViewMode;\n    // Last value seen on the ViewMode parameter. Used for UNCONTROLLED usage: we only\n    // treat ViewMode as authoritative when this changes — otherwise a parent re-render\n    // with an unchanged ViewMode would clobber a toolbar selection the user just made.\n    private GanttViewMode _lastSeenViewMode;\n    // CONTROLLED discriminator: the last value we pushed UP via ViewModeChanged (also\n    // re-synced whenever we adopt an authoritative ViewMode). A controlled parent\n    // re-render that echoes back this exact value is safe to ignore (keep the toolbar\n    // selection); any other value is an authoritative assertion (veto / reset) and must win.\n    private GanttViewMode _lastPushedViewMode;\n\n    // Snapshot of the current tasks used when the JS layer reports a drag\n    // or progress change — avoids reaching back into `Tasks` (which may have\n    // been replaced by the parent between render cycles).\n    private List<GanttTask> _tasks = new();\n\n    // The toolbar only exposes Day/Week/Month/Year. When the caller passes a\n    // QuarterDay / HalfDay view mode (or a ZoomLevels subset that excludes the\n    // current level), we keep the toggle group unselected.\n    private string? _toolbarViewValue => _currentViewMode switch\n    {\n        GanttViewMode.Day => \"Day\",\n        GanttViewMode.Week => \"Week\",\n        GanttViewMode.Month => \"Month\",\n        GanttViewMode.Year => \"Year\",\n        _ => null,\n    };\n\n    // The toolbar-representable levels, in canonical order. QuarterDay / HalfDay are\n    // deliberately excluded — they have no localized toolbar label and were never\n    // rendered as buttons (they remain settable through the ViewMode parameter).\n    private static readonly GanttViewMode[] _defaultZoomLevels =\n    {\n        GanttViewMode.Day, GanttViewMode.Week, GanttViewMode.Month, GanttViewMode.Year,\n    };\n\n    // Levels to render as toolbar buttons: the caller's ZoomLevels subset (order\n    // preserved, non-toolbar levels filtered out), else the full default set.\n    private IReadOnlyList<GanttViewMode> ResolvedZoomLevels =>\n        ZoomLevels is { Count: > 0 }\n            ? ZoomLevels.Where(l => Array.IndexOf(_defaultZoomLevels, l) >= 0).ToList()\n            : _defaultZoomLevels;\n\n    private static string ZoomLabelKey(GanttViewMode level) => level switch\n    {\n        GanttViewMode.Day => \"Gantt.Day\",\n        GanttViewMode.Week => \"Gantt.Week\",\n        GanttViewMode.Month => \"Gantt.Month\",\n        GanttViewMode.Year => \"Gantt.Year\",\n        _ => \"Gantt.Day\",\n    };\n\n    protected override void OnInitialized()\n    {\n        // DefaultZoom (when set) seeds the initial toolbar selection + the level the\n        // timeline mounts at, overriding ViewMode's default. The *-SeenViewMode /\n        // -PushedViewMode discriminators still track the ViewMode PARAMETER so the\n        // controlled/uncontrolled change-detection in OnParametersSetAsync is unchanged.\n        _currentViewMode = DefaultZoom ?? ViewMode;\n        _lastSeenViewMode = ViewMode;\n        _lastPushedViewMode = ViewMode;\n        _tasks = SortedTasks(Tasks?.ToList() ?? new List<GanttTask>());\n        _lastTasksHash = ComputeTasksHash(_tasks);\n        _lastParentHash = _lastTasksHash;\n        _lastPushedTasksHash = _lastTasksHash;\n        _lastOptionsHash = ComputeOptionsHash();\n    }\n\n    // The init-only render options (Readonly / TodayHighlight / BarHeight /\n    // ColumnWidth) used to be captured ONCE in the GanttInitAsync options bag, so\n    // changing any of them after init was silently dropped (battle-test wave 1 #2).\n    // We now fingerprint them and push the new bag to the live instance via\n    // gantt.refresh whenever the fingerprint changes.\n    private int _lastOptionsHash;\n    private int ComputeOptionsHash() => System.HashCode.Combine(Readonly, TodayHighlight, BarHeight, ColumnWidth);\n\n    // When GroupBy is set, sort tasks so that each group clusters together.\n    // The original relative order within a group is preserved (stable sort).\n    private List<GanttTask> SortedTasks(List<GanttTask> raw)\n    {\n        if (GroupBy is null) return raw;\n        return raw\n            .Select((t, i) => (t, i))\n            .OrderBy(x => GroupBy(x.t))\n            .ThenBy(x => x.i)\n            .Select(x => x.t)\n            .ToList();\n    }\n\n    protected override async Task OnParametersSetAsync()\n    {\n        // CONTROLLED (ViewModeChanged bound): adopt the incoming ViewMode whenever it\n        // differs from what we last pushed via ViewModeChanged. An echo of our own push\n        // is a no-op (keep the toolbar selection); anything else — including a veto that\n        // lands back on the pre-click parent value — is authoritative and must win, so\n        // the toolbar rolls back to it.\n        // UNCONTROLLED: only a genuine change against the last value we SAW as a\n        // parameter is authoritative — comparing against _lastSeenViewMode (not\n        // _currentViewMode) means an unchanged ViewMode on a parent re-render leaves a\n        // toolbar selection intact, so the user's zoom no longer silently reverts.\n        var viewModeChanged = ViewModeChanged.HasDelegate\n            ? ViewMode != _lastPushedViewMode\n            : ViewMode != _lastSeenViewMode;\n        if (viewModeChanged)\n        {\n            _currentViewMode = ViewMode;\n            _lastPushedViewMode = ViewMode;\n            if (_initialized && _instanceId is not null)\n                await Interop.GanttChangeViewModeAsync(_instanceId, ViewMode.ToString());\n        }\n        _lastSeenViewMode = ViewMode;\n\n        // If a previous GanttInitAsync threw, the component latched into the\n        // error state (_initError set, _initialized false, _instanceId null) and\n        // OnAfterRenderAsync stopped retrying. A later parameter change is the\n        // signal that the world may have moved on (the host element re-appeared,\n        // the JS module was reloaded, the caller supplied new Tasks/ViewMode), so\n        // clear the error to let OnAfterRenderAsync re-attempt init on the next\n        // render instead of staying permanently dead (battle-test wave 1, #19).\n        if (_initError is not null && !_initialized\n            && (viewModeChanged || Tasks is not null))\n        {\n            // Re-seed the snapshot from the latest Tasks so the retried init\n            // mounts the current data, not the stale list captured at first init.\n            _tasks = SortedTasks(Tasks?.ToList() ?? new List<GanttTask>());\n            _lastTasksHash = ComputeTasksHash(_tasks);\n            _lastParentHash = _lastTasksHash;\n            _lastPushedTasksHash = _lastTasksHash;\n            // Clearing the error makes the razor render the host element again on\n            // the render that follows OnParametersSetAsync; OnAfterRenderAsync then\n            // sees no instance + no error and re-attempts init against that host.\n            _initError = null;\n        }\n\n        if (!_initialized || _instanceId is null) return;\n\n        // Push changed init-only render options (Readonly/TodayHighlight/BarHeight/\n        // ColumnWidth) to the live instance — these are NOT folded into the tasks\n        // payload, so without this they only ever applied at init (#2).\n        var optionsHash = ComputeOptionsHash();\n        if (optionsHash != _lastOptionsHash)\n        {\n            _lastOptionsHash = optionsHash;\n            // Trim safety: anonymous types crossing JS interop lose their constructor\n            // parameter names under a trimmed publish, and JSRuntime's reflection-based\n            // serializer throws NotSupportedException(\"ConstructorContainsNullParameterNames\")\n            // at runtime (same class of bug as BlurFade/PreventDefaultKeyRule). Use a\n            // Dictionary instead — identical JSON, no constructor metadata involved.\n            await Interop.GanttRefreshAsync(_instanceId, new Dictionary<string, object?>\n            {\n                [\"readonly\"] = Readonly,\n                [\"todayHighlight\"] = TodayHighlight,\n                [\"barHeight\"] = BarHeight,\n                [\"columnWidth\"] = ColumnWidth,\n            });\n        }\n\n        if (Tasks is not null)\n        {\n            var incoming = SortedTasks(Tasks.ToList());\n            var hash = ComputeTasksHash(incoming);\n            // CONTROLLED (TasksChanged bound): gate the re-push on the PUSHED hash, not\n            // the parent hash. A controlled parent re-render that echoes back exactly\n            // the hash we last pushed via TasksChanged is a no-op (keep the in-flight\n            // drag/progress edit); anything else — including a veto that lands back on\n            // the PRE-drag parent hash — is authoritative and must win, rolling the\n            // bars back to it.\n            // UNCONTROLLED: gate on the PARENT hash, not _lastTasksHash. A local JS\n            // drag/progress edit advances _lastTasksHash (via ReplaceTask) but NOT\n            // _lastParentHash, so an uncontrolled parent re-render that still\n            // carries the original (pre-drag) Tasks is seen as \"parent unchanged\"\n            // and the bars are left where the user dropped them. Only a genuinely\n            // new parent payload (different records, or a swapped BarColor/GroupBy\n            // delegate output) moves _lastParentHash and re-pushes to JS.\n            var parentMoved = TasksChanged.HasDelegate\n                ? hash != _lastPushedTasksHash\n                : hash != _lastParentHash;\n            if (parentMoved)\n            {\n                _lastTasksHash = hash;\n                _lastPushedTasksHash = hash;\n                _tasks = incoming;\n                await Interop.GanttSetTasksAsync(_instanceId, incoming.Select(ToJsTask).Cast<object>());\n            }\n            _lastParentHash = hash;\n        }\n    }\n\n    protected override async Task OnAfterRenderAsync(bool firstRender)\n    {\n        // Init is no longer latched to firstRender. We attempt it whenever the\n        // component has no live instance yet AND is not currently parked in the\n        // error state. A failed init sets _initError, which blocks further auto-\n        // retries here until OnParametersSetAsync clears it on the next relevant\n        // parameter change — so a transient init failure is recoverable rather\n        // than permanently dead (battle-test wave 1, #19).\n        if (_initialized || _instanceId is not null || _initError is not null || _initializing) return;\n\n        // Reuse the DotNetObjectReference across retries so a second attempt does\n        // not leak the first one.\n        _selfRef ??= DotNetObjectReference.Create(this);\n\n        // Trim safety: see the GanttRefreshAsync comment above — anonymous types crossing\n        // JS interop crash under a trimmed publish, so this options bag (and ToJsTask\n        // below) use Dictionary<string, object?> instead.\n        var options = new Dictionary<string, object?>\n        {\n            [\"tasks\"] = _tasks.Select(ToJsTask).ToArray(),\n            // Mount at the effective current level so DefaultZoom is honoured on first paint.\n            [\"viewMode\"] = _currentViewMode.ToString(),\n            [\"readonly\"] = Readonly,\n            [\"todayHighlight\"] = TodayHighlight,\n            [\"barHeight\"] = BarHeight,\n            [\"columnWidth\"] = ColumnWidth,\n        };\n\n        _initializing = true;\n        try\n        {\n            _instanceId = await Interop.GanttInitAsync(_hostRef, _selfRef, options);\n            _initialized = true;\n        }\n        catch (Microsoft.JSInterop.JSDisconnectedException)\n        {\n            // Circuit was torn down between firstRender starting and\n            // GanttInitAsync resolving. No init-error UI to show because the\n            // user is already gone — match the pattern other components use.\n        }\n        catch (Microsoft.JSInterop.JSException ex)\n        {\n            _initError = $\"Gantt initialization failed: {ex.Message}\";\n            Console.Error.WriteLine($\"[Lumeo Gantt] Init failed: {ex.Message}\");\n            if (OnInitError.HasDelegate)\n                await OnInitError.InvokeAsync(ex.Message);\n            StateHasChanged();\n        }\n        finally\n        {\n            _initializing = false;\n        }\n    }\n\n    private object ToJsTask(GanttTask t)\n    {\n        // For milestones render as zero-duration (start == end) so the renderer\n        // picks them up and draws a diamond shape.\n        var end = t.IsMilestone ? t.Start : t.End;\n        // Trim safety: Dictionary instead of an anonymous type — see the GanttRefreshAsync\n        // comment above.\n        return new Dictionary<string, object?>\n        {\n            [\"id\"] = t.Id,\n            [\"name\"] = t.Name,\n            [\"start\"] = t.Start.ToString(\"yyyy-MM-dd\"),\n            [\"end\"] = end.ToString(\"yyyy-MM-dd\"),\n            [\"progress\"] = t.Progress,\n            [\"dependencies\"] = t.Dependencies ?? Array.Empty<string>(),\n            [\"custom_class\"] = t.CustomClass,\n            [\"is_milestone\"] = t.IsMilestone,\n            [\"group_label\"] = t.GroupLabel,\n            [\"bar_color\"] = BarColor?.Invoke(t),\n        };\n    }\n\n    // Instance (not static) because the change-detection has to fold in the\n    // per-task OUTPUT of the BarColor and GroupBy delegates. Those delegates are\n    // the only task properties whose rendered result lives outside the GanttTask\n    // record itself: a parent can swap the BarColor lambda (recolour every bar)\n    // or the GroupBy lambda (re-cluster the lanes) while leaving the task list\n    // value-identical. Both feed the per-task JS payload (bar_color / group_label\n    // + the SortedTasks order), so re-pushing tasks via GanttSetTasksAsync applies\n    // them — but only if the hash actually moves when the delegate output does.\n    private int ComputeTasksHash(IEnumerable<GanttTask> tasks)\n    {\n        // Cheap identity hash — good enough to detect \"did the caller swap the\n        // tasks list (or recolour/regroup it)?\" without a JS round-trip on every\n        // render.\n        var hash = new HashCode();\n        foreach (var t in tasks)\n        {\n            hash.Add(t.Id);\n            hash.Add(t.Name);\n            hash.Add(t.Start);\n            hash.Add(t.End);\n            hash.Add(t.Progress);\n            hash.Add(t.CustomClass);\n            hash.Add(t.IsMilestone);\n            hash.Add(t.GroupLabel);\n            // Bug fix (Codex round 2, P1 #1): ParentId was never folded into this\n            // hash, so a ParentId-ONLY change (e.g. re-parenting a task in a\n            // GanttV3-hierarchy consumer, then handing the SAME Tasks list back\n            // down) hashed identically to the unchanged task — the change-gates\n            // above (parentMoved / the controlled/uncontrolled discriminators)\n            // saw \"no change\" and silently kept the OLD ParentId, so the next\n            // drag/resize/progress edit (via ReplaceTask) persisted the stale\n            // parent instead of the caller's update.\n            hash.Add(t.ParentId);\n            if (t.Dependencies is not null)\n                foreach (var d in t.Dependencies) hash.Add(d);\n            // Fold the delegate output so a BarColor / GroupBy swap that leaves the\n            // GanttTask records untouched is still detected and re-pushed.\n            hash.Add(BarColor?.Invoke(t));\n            hash.Add(GroupBy?.Invoke(t));\n        }\n        return hash.ToHashCode();\n    }\n\n    private async Task OnViewModeChangedAsync(string? v)\n    {\n        if (string.IsNullOrEmpty(v)) return;\n        if (!Enum.TryParse<GanttViewMode>(v, out var parsed)) return;\n        if (parsed == _currentViewMode) return;\n\n        _currentViewMode = parsed;\n        // Deliberately do NOT touch _lastSeenViewMode here: it tracks the value\n        // the *parent* last passed via ViewMode. Leaving it untouched means a\n        // subsequent parent re-render that still carries the original (constant)\n        // ViewMode is seen as \"unchanged\" and no longer clobbers this toolbar\n        // selection — which is exactly the revert bug. Uncontrolled usage relies on\n        // this; controlled (@bind-ViewMode) usage instead re-syncs through\n        // OnParametersSetAsync via _lastPushedViewMode below.\n\n        // Record _lastPushedViewMode BEFORE invoking ViewModeChanged: a controlled\n        // parent re-renders synchronously inside InvokeAsync, so OnParametersSetAsync\n        // must already see this value as \"the value we just pushed\" to distinguish an\n        // accepted echo (keep the toolbar selection) from a veto (parent kept its old\n        // value -> roll back).\n        _lastPushedViewMode = parsed;\n\n        // Keep a bound parent's ViewMode in lock-step with the toolbar. Without\n        // a binding this is a no-op and the component stays uncontrolled.\n        if (ViewModeChanged.HasDelegate)\n            await ViewModeChanged.InvokeAsync(parsed);\n\n        // The renderer is only present once JS init has handed back an id.\n        if (_instanceId is not null)\n            await Interop.GanttChangeViewModeAsync(_instanceId, parsed.ToString());\n    }\n\n    [JSInvokable]\n    public async Task JsOnTaskClick(GanttTask t)\n    {\n        // Bug fix (Codex round 2, P2 #2): `t` is deserialized straight from\n        // gantt-v2.js's taskToJson payload, which never serializes ParentId (v2\n        // has no hierarchy concept) — same root cause ReplaceTask's ParentId\n        // merge fixes for JsOnDateChange/JsOnProgressChange, so OnTaskClick's own\n        // argument always reported ParentId == null for a GanttV3-hierarchy\n        // consumer, even though the task's ACTUAL ParentId (in _tasks) was set.\n        //\n        // Bug fix (Codex round 6, P2 #5): the original fix substituted the\n        // WHOLE stored task (`current ?? t`) instead of merging just the one\n        // missing field — but `t` here is gantt-v2.js's own RENDERER-NORMALIZED\n        // payload (e.g. normalizeTasks forces a milestone's `end = start`\n        // before taskToJson ever serializes it — see GanttBar.razor's own\n        // remarks on this same v2 normalization for the round-3 tooltip fix),\n        // while `current` (looked up from `_tasks`) is whatever the CONSUMER\n        // originally supplied, which nothing enforces stays consistent (e.g.\n        // a milestone's End field can differ from its Start). Replacing `t`\n        // outright with `current` silently threw that normalization away —\n        // a milestone click could report an inconsistent multi-day End again.\n        // Only ParentId is actually missing from `t`; merging JUST that field\n        // onto the normalized payload fixes the hierarchy gap without losing\n        // the normalization the renderer already did.\n        var stored = _tasks.Find(x => x.Id == t.Id);\n        var merged = stored is not null ? t with { ParentId = stored.ParentId } : t;\n        if (OnTaskClick.HasDelegate) await OnTaskClick.InvokeAsync(merged);\n    }\n\n    [JSInvokable]\n    public async Task JsOnDateChange(GanttTask t)\n    {\n        // Fix (Codex review wave, round 2): ReplaceTask's ParentId merge only\n        // reached _tasks/TasksChanged before — `t` here is a BY-VALUE local, so\n        // `t = t with {...}` inside ReplaceTask reassigned ReplaceTask's own\n        // copy, never this caller's. Using the RETURN VALUE for OnDateChange\n        // is what actually carries the merge to that callback's argument.\n        var merged = ReplaceTask(t);\n        if (OnDateChange.HasDelegate) await OnDateChange.InvokeAsync(merged);\n        if (TasksChanged.HasDelegate)\n        {\n            // Record _lastPushedTasksHash BEFORE invoking TasksChanged: a controlled\n            // parent re-renders synchronously inside InvokeAsync, so OnParametersSetAsync\n            // must already see this hash as \"the value we just pushed\" to distinguish an\n            // accepted echo (keep the in-flight edit) from a veto (parent kept its old\n            // value -> roll back).\n            _lastPushedTasksHash = _lastTasksHash;\n            await TasksChanged.InvokeAsync(_tasks.ToList());\n        }\n    }\n\n    [JSInvokable]\n    public async Task JsOnProgressChange(GanttTask t)\n    {\n        // See JsOnDateChange — same ParentId-merge return-value fix.\n        var merged = ReplaceTask(t);\n        if (OnProgressChange.HasDelegate) await OnProgressChange.InvokeAsync(merged);\n        if (TasksChanged.HasDelegate)\n        {\n            // See JsOnDateChange — record the pushed-hash discriminator before invoking.\n            _lastPushedTasksHash = _lastTasksHash;\n            await TasksChanged.InvokeAsync(_tasks.ToList());\n        }\n    }\n\n    private GanttTask ReplaceTask(GanttTask t)\n    {\n        var idx = _tasks.FindIndex(x => x.Id == t.Id);\n        if (idx >= 0)\n        {\n            // Bug fix (Codex review wave; C#-only — gantt-v2.js itself is\n            // untouched, a recorded exception to the v2-untouched rule):\n            // taskToJson (gantt-v2.js) never serializes ParentId at all — v2\n            // has no hierarchy concept — so `t`, deserialized from the JS\n            // drag/resize/progress payload, always has ParentId == null.\n            // Replacing wholesale would silently drop it on every edit for a\n            // task a GanttV3-hierarchy consumer had set it on. Preserve the\n            // CURRENT task's ParentId across the replace instead. Returned\n            // (not just stored in _tasks) so JsOnDateChange/JsOnProgressChange\n            // can pass the MERGED task to OnDateChange/OnProgressChange too —\n            // `t` is a by-value parameter, so reassigning it here never\n            // propagates back to either caller's own local on its own.\n            t = t with { ParentId = _tasks[idx].ParentId };\n            _tasks[idx] = t;\n        }\n        _lastTasksHash = ComputeTasksHash(_tasks);\n        return t;\n    }\n\n    public async ValueTask DisposeAsync()\n    {\n        if (_instanceId is not null)\n        {\n            try { await Interop.GanttDestroyAsync(_instanceId); }\n            catch (Microsoft.JSInterop.JSDisconnectedException) { }\n        }\n        _selfRef?.Dispose();\n    }\n}\n"
    },
    {
      "path": "UI/Gantt/GanttTypes.cs",
      "content": "namespace Lumeo;\n\n/// <summary>\n/// A task bar rendered on the Gantt chart. Used as the data contract between\n/// Blazor and the Lumeo SVG Gantt renderer.\n/// </summary>\npublic record GanttTask(\n    string Id,\n    string Name,\n    DateTime Start,\n    DateTime End,\n    int Progress = 0,                // 0-100\n    string[]? Dependencies = null,   // array of Task Ids\n    string? CustomClass = null,\n    bool IsMilestone = false,        // renders as a diamond; zero-duration point event\n    string? GroupLabel = null        // optional swim-lane / group header label\n)\n{\n    // Trim safety: this record is deserialized from JS (JsOnTaskClick/JsOnDateChange/\n    // JsOnProgressChange [JSInvokable] parameters). JSRuntime's reflection-based\n    // serializer must never bind the positional ctor — the trimmer strips its parameter\n    // names (\"ConstructorContainsNullParameterNames\", crashes the component under a\n    // trimmed publish). With this parameterless ctor STJ uses property-based\n    // (de)serialization instead. Do not remove.\n    public GanttTask() : this(\"\", \"\", default, default) { }\n\n    /// <summary>\n    /// Optional parent task id, enabling GanttV3's tree-pane hierarchy (design spec\n    /// \"Public API\" &gt; Additive: \"GanttTask.ParentId (new optional field -&gt;\n    /// hierarchy; GroupBy keeps working as flat grouping)\"). Null (the default) means\n    /// a root-level task — the same as every existing task before this field existed.\n    ///\n    /// Deliberately added as a non-positional <c>init</c> property in the record body\n    /// rather than a new positional-constructor parameter: the positional constructor\n    /// and <c>Deconstruct</c> are already-shipped public API (PublicAPI.Shipped.txt),\n    /// and CONTRIBUTING.md's API-stability policy forbids changing a shipped member's\n    /// signature outside the Obsolete-one-minor-then-major flow. A body property is\n    /// purely additive — it does not touch the existing constructor/Deconstruct\n    /// signatures, so v2 (which never sets or reads it) keeps compiling and behaving\n    /// identically. It IS still picked up by System.Text.Json's property-based\n    /// (de)serialization (the parameterless ctor above), by the compiler-generated\n    /// record Equals/GetHashCode (so <see cref=\"GanttTask\"/> equality still reflects a\n    /// ParentId-only change), and by <c>with</c>-expressions.\n    /// </summary>\n    public string? ParentId { get; init; }\n}\n\n/// <summary>\n/// Gantt timeline zoom level. Supported by the Lumeo SVG engine:\n/// QuarterDay = 6-hour columns, HalfDay = 12-hour columns,\n/// Day = 1 column/day, Week = 1 column/week,\n/// Month = 1 column/month, Year = 1 column/year.\n/// </summary>\npublic enum GanttViewMode\n{\n    QuarterDay,\n    HalfDay,\n    Day,\n    Week,\n    Month,\n    Year\n}\n"
    }
  ],
  "slots": [
    {
      "name": "TrailingContent",
      "owner": "Gantt",
      "type": "RenderFragment?",
      "description": "Extra content rendered at the end of the toolbar row, alongside the zoom-level buttons."
    }
  ],
  "serviceDependencies": [
    {
      "service": "IComponentInteropService",
      "namespace": "Lumeo.Services"
    },
    {
      "service": "ILumeoLocalizer",
      "namespace": "Lumeo.Services.Localization"
    }
  ],
  "cascadingDependencies": [],
  "relatedComponents": [
    {
      "name": "ToggleGroup",
      "reason": "used internally"
    }
  ],
  "keyboardInteractions": [],
  "tests": [
    "tests/Lumeo.Tests.E2E/Gantt/GanttDayModeMath.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttDragParityTests.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttParityTestBase.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttParityTests.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttV3ArrowVirtualizationTests.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttV3ClickActivationTests.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttV3NavTests.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttV3RtlTests.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttV3ScrollCenteringTests.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttV3StickyHeaderTests.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttV3StickyLeftTests.cs",
    "tests/Lumeo.Tests.E2E/Gantt/GanttV3TreeTests.cs",
    "tests/Lumeo.Tests.E2E/Visual/GanttParityVisualTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttControlledRollbackTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttInitRecoveryTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttInteropTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttKeyboardTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttRefreshOptionsTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttTaskParentIdTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttTasksHashTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttUncontrolledDragTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttViewModeControlledTests.cs",
    "tests/Lumeo.Tests/Components/Gantt/GanttZoomLevelsTests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttArrowRoutingTests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttRowModelTests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttScaleTests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttStateTests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttTreeAndArrowsTests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound10Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound11Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound12Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound13Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound14Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound15Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound16Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound17Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound2Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound3Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound4Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound5Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound6Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound7Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound8Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CodexRound9Tests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3CreateTests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3DragTests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3ProgressClickCanDropTests.cs",
    "tests/Lumeo.Tests/Components/GanttV3/GanttV3ReadonlyGuardTests.cs",
    "tests/Lumeo.Tests/Components/Localization/P2LocalizationSweepTests.cs",
    "tests/Lumeo.Tests/Components/Scrollspy/ScrollspyOneWayHost.razor",
    "tests/Lumeo.Tests/Theming/RadiusTokenGuardTests.cs"
  ],
  "mdSummary": "# Gantt\n\nGantt component.\n\n**Package:** `Lumeo.Gantt` · **Category:** Data Display/Lists & Trees\n**Docs:** https://lumeo.nativ.sh/components/gantt\n\n## Parameters\n| Owner | Name | Type | Default | Description |\n|---|---|---|---|---|\n| Gantt | Tasks | `IEnumerable<GanttTask>?` | `` | The tasks (and milestones) rendered as bars. Two-way bindable via TasksChanged, which also fires after an in-chart drag/resize/progress edit so the parent can persist it. |\n| Gantt | TasksChanged | `EventCallback<IEnumerable<GanttTask>>` | `` | Raised whenever the task set changes — either because the parent's Tasks genuinely changed, or because the user dragged a bar / changed its progress in the chart. |\n| Gantt | ViewMode | `GanttViewMode` | `GanttViewMode.Day` | The active zoom level (Day/Week/Month/Year, plus the non-toolbar QuarterDay/HalfDay). Two-way bindable via ViewModeChanged; without a binding the component still tracks toolbar selections on its own. Default Day. |\n| Gantt | ViewModeChanged | `EventCallback<GanttViewMode>` | `` | Raised when the user picks a new zoom level from the toolbar. Bind it (@bind-ViewMode) to keep the parent's ViewMode in lock-step with the toolbar; without a binding the component still tracks the toolbar selection on its own (uncontrolled usage). |\n| Gantt | ZoomLevels | `IReadOnlyList<GanttViewMode>?` | `` | Restricts which zoom buttons the toolbar renders, in the given order. When null or empty the full default set (Day, Week, Month, Year) is shown — matching the previous hardcoded toolbar. Only toolbar-representable levels are drawn: QuarterDay / HalfDay have no toolbar button and are silently skipped here (still settable via ViewMode). Example: ZoomLevels=\"new[]{ GanttViewMode.Day, GanttViewMode.Week, GanttViewMode.Month }\". |\n| Gantt | DefaultZoom | `GanttViewMode?` | `` | The zoom level selected on first render. When set it seeds the initial toolbar selection (and the level the timeline mounts at), overriding ViewMode's default — handy for uncontrolled usage, e.g. ZoomLevels=\"…\" DefaultZoom=\"GanttViewMode.Week\". After mount, ViewMode / @bind-ViewMode govern as before. Null falls back to ViewMode (the existing behaviour). |\n| Gantt | OnTaskClick | `EventCallback<GanttTask>` | `` | Raised when a task bar is clicked, with the clicked task. |\n| Gantt | OnDateChange | `EventCallback<GanttTask>` | `` | Raised after a task bar's start/end dates change via drag, with the updated task. Also folded into TasksChanged. |\n| Gantt | OnProgressChange | `EventCallback<GanttTask>` | `` | Raised after a task bar's progress changes via drag, with the updated task. Also folded into TasksChanged. |\n| Gantt | Height | `string` | `\"500px\"` | CSS max-height of the chart's scrollable viewport (a CSS length string, e.g. \"500px\"). The chart shrinks to its content and only scrolls when the task list exceeds this height. Default \"500px\". |\n| Gantt | Readonly | `bool` | `false` | Disables drag-to-reschedule and drag-to-progress editing in the underlying chart. |\n| Gantt | TodayHighlight | `bool` | `true` | Highlight today with a vertical line and dot marker. Default true. |\n| Gantt | BarHeight | `int?` | `` | Override the pixel height of each task bar (default 22). |\n| Gantt | ColumnWidth | `int?` | `` | Override the pixel width of each date column for the current view mode. |\n| Gantt | BarColor | `Func<GanttTask, string?>?` | `` | Per-task bar colour override. Return a CSS colour string (hex, rgb, or a CSS variable reference like \"var(--color-destructive)\") or null to use the primary theme colour. |\n| Gantt | GroupBy | `Func<GanttTask, string>?` | `` | Optional grouping function. When set, tasks are sorted by group label and a visual section-header row is injected before the first task in each group. |\n| Gantt | TrailingContent | `RenderFragment?` | `` | Extra content rendered at the end of the toolbar row, alongside the zoom-level buttons. |\n| Gantt | OnInitError | `EventCallback<string>` | `` | Raised when the underlying JS chart fails to initialize, with the error message. The component also shows a destructive-styled error box in place of the chart and retries init on the next relevant parameter change. |\n| Gantt | Class | `string?` | `` | Additional CSS classes merged onto the root container. |\n| Gantt | AdditionalAttributes | `Dictionary<string, object>?` | `` | Captures any unmatched attributes and applies them to the root container. |\n\n## Slots\n- `TrailingContent` (Gantt) — Extra content rendered at the end of the toolbar row, alongside the zoom-level buttons.\n\n## Events\n- `TasksChanged` (`EventCallback<IEnumerable<GanttTask>>`) — Raised whenever the task set changes — either because the parent's Tasks genuinely changed, or because the user dragged a bar / changed its progress in the chart.\n- `ViewModeChanged` (`EventCallback<GanttViewMode>`) — Raised when the user picks a new zoom level from the toolbar. Bind it (@bind-ViewMode) to keep the parent's ViewMode in lock-step with the toolbar; without a binding the component still tracks the toolbar selection on its own (uncontrolled usage).\n- `OnTaskClick` (`EventCallback<GanttTask>`) — Raised when a task bar is clicked, with the clicked task.\n- `OnDateChange` (`EventCallback<GanttTask>`) — Raised after a task bar's start/end dates change via drag, with the updated task. Also folded into TasksChanged.\n- `OnProgressChange` (`EventCallback<GanttTask>`) — Raised after a task bar's progress changes via drag, with the updated task. Also folded into TasksChanged.\n- `OnInitError` (`EventCallback<string>`) — Raised when the underlying JS chart fails to initialize, with the error message. The component also shows a destructive-styled error box in place of the chart and retries init on the next relevant parameter change.\n\n## Sub-components\n- (none)\n\n## Cascading values it expects\n- (none)\n\n## Services it injects\n- `IComponentInteropService` (Lumeo.Services)\n- `ILumeoLocalizer` (Lumeo.Services.Localization)\n\n## Keyboard\n—\n\n## Example\n```razor\n<Gantt />\n```\n\n## CSS variables\n`--color-border`, `--color-card`, `--color-destructive`, `--radius`\n\n## Files\n- `UI/Gantt/Gantt.razor`\n- `UI/Gantt/GanttTypes.cs`\n"
}