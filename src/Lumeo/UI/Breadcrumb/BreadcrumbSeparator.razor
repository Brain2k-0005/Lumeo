@namespace Lumeo

<li role="presentation" aria-hidden="true" class="@CssClass" @attributes="AdditionalAttributes">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else
    {
        <SvgGlyph Svg="@(LumeoIcons.ChevronRight)" class="h-3.5 w-3.5 [dir=rtl]:rotate-180" />
    }
</li>

@code {
    /// <summary>Custom separator content. When null, renders a default chevron icon (mirrored in RTL).</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>Additional CSS classes merged onto the root <c>li</c> element.</summary>
    [Parameter] public string? Class { get; set; }

    /// <summary>Unmatched attributes are splatted onto the root <c>li</c> element.</summary>
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private string CssClass => Cx.Merge("[&>svg]:h-3.5 [&>svg]:w-3.5", Class);
}
