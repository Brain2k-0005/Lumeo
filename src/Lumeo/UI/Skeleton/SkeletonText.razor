@namespace Lumeo

<div class="@CssClass" @attributes="AdditionalAttributes">
    @for (var i = 0; i < Lines; i++)
    {
        <div class="@LineClasses(i)"></div>
    }
</div>

@code {
    [Parameter] public int Lines { get; set; } = 3;
    [Parameter] public string? Class { get; set; }
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private string CssClass => $"space-y-2 {Class}".Trim();

    private string LineClasses(int index) =>
        $"animate-pulse rounded-md bg-primary/10 h-4 {(index == Lines - 1 ? "w-3/4" : "w-full")}";
}
