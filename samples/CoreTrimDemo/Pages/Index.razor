@page "/"

<h1>Lumeo core trim proof</h1>

<p>Exactly one core component (<code>Button</code>) is referenced. Per #354, the published
   <code>Lumeo.dll.br</code> must now be smaller than the pre-fix baseline (687,723 bytes,
   which was identical whether an app used one component or all 144).</p>

<Button>Click me</Button>

@code {
    // A single concrete component reference only — no reflection, no DynamicComponent.
}
