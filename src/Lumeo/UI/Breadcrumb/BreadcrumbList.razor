@namespace Lumeo

<ol class="@CssClass" @attributes="AdditionalAttributes">
    @ChildContent
</ol>

@code {
    /// <summary>The <see cref="BreadcrumbItem"/> and <see cref="BreadcrumbSeparator"/> elements that make up the trail.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>Additional CSS classes merged onto the root <c>ol</c> element.</summary>
    [Parameter] public string? Class { get; set; }

    /// <summary>Unmatched attributes are splatted onto the root <c>ol</c> element.</summary>
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private string CssClass => Cx.Merge("flex flex-wrap items-center gap-1.5 break-words text-sm text-muted-foreground sm:gap-2.5", Class);
}
