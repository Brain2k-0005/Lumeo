{
  "name": "lumeo-blazor-build",
  "private": true,
  "description": "Internal Tailwind build for Lumeo's pre-compiled utility stylesheet shipped in the NuGet package. Kept at repo root so the Razor SDK's implicit content globs in src/Lumeo/ never pick these files up.",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./src/Lumeo/Styles/lumeo-utilities.src.css -o ./src/Lumeo/wwwroot/css/lumeo-utilities.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./src/Lumeo/Styles/lumeo-utilities.src.css -o ./src/Lumeo/wwwroot/css/lumeo-utilities.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0"
  }
}
