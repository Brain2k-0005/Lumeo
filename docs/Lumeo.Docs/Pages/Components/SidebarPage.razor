@page "/components/sidebar"

<PageTitle>Sidebar — Lumeo</PageTitle>

<PageHeader Title="Sidebar" Description="A collapsible sidebar layout with groups, menus, header, and footer." />

<div class="space-y-8">
    <ComponentDemo Title="Default" Code="@_defaultCode">
        <div class="h-[400px] rounded-lg border overflow-hidden">
            <SidebarProvider @bind-IsCollapsed="_isCollapsed">
                <SidebarComponent>
                    <SidebarHeader>
                        <div class="flex items-center gap-2 px-2">
                            <div class="h-6 w-6 rounded bg-primary"></div>
                            <span class="font-semibold text-sm text-foreground">Acme Inc</span>
                        </div>
                    </SidebarHeader>
                    <SidebarContent>
                        <SidebarGroup>
                            <SidebarGroupLabel>Platform</SidebarGroupLabel>
                            <SidebarMenu>
                                <SidebarMenuItem>
                                    <SidebarMenuButton Href="#" IsActive="true">
                                        <Blazicon Svg="Lucide.House" class="h-4 w-4" />
                                        <span>Dashboard</span>
                                    </SidebarMenuButton>
                                </SidebarMenuItem>
                                <SidebarMenuItem>
                                    <SidebarMenuButton Href="#">
                                        <Blazicon Svg="Lucide.Settings" class="h-4 w-4" />
                                        <span>Settings</span>
                                    </SidebarMenuButton>
                                </SidebarMenuItem>
                            </SidebarMenu>
                        </SidebarGroup>
                        <SidebarSeparator />
                        <SidebarGroup>
                            <SidebarGroupLabel>Projects</SidebarGroupLabel>
                            <SidebarMenu>
                                <SidebarMenuItem>
                                    <SidebarMenuButton Href="#">
                                        <Blazicon Svg="Lucide.Star" class="h-4 w-4" />
                                        <span>Favorites</span>
                                    </SidebarMenuButton>
                                </SidebarMenuItem>
                            </SidebarMenu>
                        </SidebarGroup>
                    </SidebarContent>
                    <SidebarFooter>
                        <div class="flex items-center gap-2 px-2">
                            <Avatar Src="" Fallback="CN" />
                            <span class="text-sm text-foreground">User</span>
                        </div>
                    </SidebarFooter>
                </SidebarComponent>
                <main class="flex-1 p-6">
                    <SidebarTrigger />
                    <p class="text-sm text-muted-foreground mt-4">Main content area. Use the trigger button to collapse the sidebar.</p>
                </main>
            </SidebarProvider>
        </div>
    </ComponentDemo>
</div>

@code {
    private bool _isCollapsed;

    private string _defaultCode = @"<SidebarProvider @bind-IsCollapsed=""_isCollapsed"">
    <SidebarComponent>
        <SidebarHeader>
            <span class=""font-semibold text-sm"">Acme Inc</span>
        </SidebarHeader>
        <SidebarContent>
            <SidebarGroup>
                <SidebarGroupLabel>Platform</SidebarGroupLabel>
                <SidebarMenu>
                    <SidebarMenuItem>
                        <SidebarMenuButton Href=""#"" IsActive=""true"">
                            <Blazicon Svg=""Lucide.House"" class=""h-4 w-4"" />
                            <span>Dashboard</span>
                        </SidebarMenuButton>
                    </SidebarMenuItem>
                </SidebarMenu>
            </SidebarGroup>
        </SidebarContent>
        <SidebarFooter>
            <Avatar Src="""" Fallback=""CN"" />
        </SidebarFooter>
    </SidebarComponent>
    <main class=""flex-1 p-6"">
        <SidebarTrigger />
        <p>Main content area.</p>
    </main>
</SidebarProvider>";
}
