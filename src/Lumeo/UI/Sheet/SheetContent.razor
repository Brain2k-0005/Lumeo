@namespace Lumeo
@using Lumeo.Services
@implements IAsyncDisposable

@if (Shell is not null)
{
    @* OverlayProvider already rendered the SheetContent shell around our parent
       component — rendering another shell here would stack a second backdrop +
       sliding panel on top. Pass the children straight through to the host
       shell instead. Side / Size / PreventClose / Class on this nested element
       are intentionally ignored; the provider controls those via OverlayOptions. *@
    @ChildContent
}
else if (Context.IsOpen)
{
    <div class="fixed inset-0 z-50">
        <div class="fixed inset-0 bg-black/80 animate-fade-in" @onclick="HandleBackdropClick"></div>
        <div id="@_contentId" class="@CssClass" role="dialog" aria-modal="true" aria-labelledby="@Context.TitleId" aria-describedby="@Context.DescriptionId" @onkeydown="HandleKeyDown" @onanimationend="HandleAnimationEnd" tabindex="-1" @attributes="AdditionalAttributes">
            @ChildContent
            @if (!PreventClose)
            {
                <button class="absolute right-4 top-4 rounded-sm opacity-70 transition-opacity hover:opacity-100 focus:outline-none" @onclick="Close">
                    <Blazicon Svg="Lucide.X" class="h-4 w-4" />
                    <span class="sr-only">@L["Dialog.Close"]</span>
                </button>
            }
        </div>
    </div>
}

@inject ComponentInteropService Interop
@inject Lumeo.Services.Localization.ILumeoLocalizer L

@code {
    [CascadingParameter] public Sheet.SheetContext Context { get; set; } = default!;
    [CascadingParameter] public OverlayShellMarker? Shell { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public SheetSide Side { get; set; } = SheetSide.Right;
    [Parameter] public SheetSize Size { get; set; } = SheetSize.Default;
    /// <summary>How the sheet enters/leaves. <c>Slide</c> (default) animates
    /// from the configured <see cref="Side"/>. <c>Fade</c> uses a simple
    /// opacity tween — useful in dense forms where popovers and animations
    /// compete. <c>None</c> disables the animation entirely.</summary>
    [Parameter] public SheetAnimation Animation { get; set; } = SheetAnimation.Slide;
    [Parameter] public bool PreventClose { get; set; }
    [Parameter] public string? Class { get; set; }
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private readonly string _contentId = $"sheet-{Guid.NewGuid():N}";
    private bool _wasOpen;
    // After the slide animation ends, drop the animate-slide-in-* class so
    // animation-fill-mode: both no longer forces an identity transform
    // matrix on the element — that matrix is what creates the containing
    // block that breaks position:fixed popovers (Select / DatePicker /
    // Combobox) inside the sheet. See lumeo.css for the spec rationale.
    private bool _animationDone;

    private string CssClass => $"fixed z-50 flex flex-col gap-4 bg-background p-6 shadow-lg {PositionClasses} {ActiveAnimationClass} {Class}".Trim();

    // ActiveAnimationClass returns the animation class only while the
    // animation is still running. Once @onanimationend fires we set
    // _animationDone=true and the class drops out — Chrome / Firefox /
    // Safari then compute the element's transform back to `none`,
    // restoring the viewport as the containing block for descendants.
    private string ActiveAnimationClass => _animationDone ? "" : AnimationClass;

    private string SizeClass => Side is SheetSide.Left or SheetSide.Right
        ? Size switch
        {
            SheetSize.Sm => "sm:max-w-xs",
            SheetSize.Lg => "sm:max-w-lg",
            SheetSize.Xl => "sm:max-w-xl",
            SheetSize.Full => "sm:max-w-full",
            _ => "sm:max-w-sm"
        }
        : "";

    private string PositionClasses => Side switch
    {
        SheetSide.Top => "inset-x-0 top-0",
        SheetSide.Bottom => "inset-x-0 bottom-0",
        SheetSide.Left => $"inset-y-0 left-0 h-full w-3/4 {SizeClass}",
        SheetSide.Right => $"inset-y-0 right-0 h-full w-3/4 {SizeClass}",
        _ => $"inset-y-0 right-0 h-full w-3/4 {SizeClass}"
    };

    // Animation=Slide is paired with @onanimationend → drop class trick
    // (see _animationDone) so the post-animation transform doesn't trap
    // descendant popovers. Fade / None are also fine; they don't use a
    // transform at all so no trick is needed.
    private string AnimationClass => Animation switch
    {
        SheetAnimation.None => "",
        SheetAnimation.Fade => "animate-fade-in",
        SheetAnimation.Slide => Side switch
        {
            SheetSide.Top => "animate-slide-in-from-top",
            SheetSide.Bottom => "animate-slide-in-from-bottom",
            SheetSide.Left => "animate-slide-in-from-left",
            SheetSide.Right => "animate-slide-in-from-right",
            _ => "animate-slide-in-from-right"
        },
        _ => ""
    };

    // Kept for backward-compat with tests / consumers that read SideClasses;
    // returns the same composed string the older versions returned.
    private string SideClasses => $"{PositionClasses} {ActiveAnimationClass}".Trim();

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        // When passthrough mode is active the host SheetContent owns scroll lock
        // + focus trap; this nested instance is a no-op for lifecycle effects.
        if (Shell is not null) return;

        if (Context.IsOpen && !_wasOpen)
        {
            // Reset the animation flag every time we re-open so the
            // entrance animation runs fresh.
            _animationDone = false;
            await Interop.LockScroll();
            await Interop.SetupFocusTrap(_contentId);
            _wasOpen = true;
        }
        else if (!Context.IsOpen && _wasOpen)
        {
            await Cleanup();
            _wasOpen = false;
        }
    }

    private void HandleAnimationEnd()
    {
        if (_animationDone) return;
        _animationDone = true;
        StateHasChanged();
    }

    private async Task Close() => await Context.SetOpen.InvokeAsync(false);

    private async Task HandleBackdropClick()
    {
        if (!PreventClose) await Close();
    }

    private async Task HandleKeyDown(KeyboardEventArgs e)
    {
        if (e.Key == "Escape" && !PreventClose) await Close();
    }

    private async Task Cleanup()
    {
        try
        {
            await Interop.RemoveFocusTrap(_contentId);
            await Interop.UnlockScroll();
        }
        catch (JSDisconnectedException) { }
    }

    public async ValueTask DisposeAsync()
    {
        if (_wasOpen) await Cleanup();
    }

    public enum SheetSide { Top, Right, Bottom, Left }
    public enum SheetSize { Sm, Default, Lg, Xl, Full }
    public enum SheetAnimation { Slide, Fade, None }
}
