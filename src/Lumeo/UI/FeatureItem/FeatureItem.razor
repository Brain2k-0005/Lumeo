@namespace Lumeo

<div class="@CssClass" @attributes="AdditionalAttributes">
    @if (IconContent is not null)
    {
        <div class="mb-4 inline-flex h-10 w-10 items-center justify-center rounded-lg bg-primary/10 text-primary">
            @IconContent
        </div>
    }
    @if (!string.IsNullOrEmpty(Title))
    {
        <h3 class="text-lg font-semibold text-foreground">@Title</h3>
    }
    @if (!string.IsNullOrEmpty(Description))
    {
        <p class="mt-2 text-sm text-muted-foreground">@Description</p>
    }
    @ChildContent
</div>

@code {
    /// <summary>Feature heading, rendered as a bold <c>h3</c> below the icon badge.</summary>
    [Parameter] public string? Title { get; set; }
    /// <summary>Supporting copy shown muted below <see cref="Title"/>.</summary>
    [Parameter] public string? Description { get; set; }

    /// <summary>
    /// The icon slot rendered in the leading badge. Named <c>IconContent</c>
    /// (not <c>Icon</c>) so the <see cref="Lumeo.Icon"/> component can be used
    /// freely inside it — a parameter called <c>Icon</c> shadowed the component
    /// type, turning <c>&lt;Icon&gt;</c> into the slot rather than the icon (#300).
    /// </summary>
    [Parameter] public RenderFragment? IconContent { get; set; }
    /// <summary>Extra content rendered after <see cref="Description"/> — e.g. a link or
    /// list of details.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
    /// <summary>Additional CSS classes merged onto the root <c>div</c>.</summary>
    [Parameter] public string? Class { get; set; }
    /// <summary>Unmatched attributes splatted onto the root <c>div</c>.</summary>
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private string CssClass => Cx.Merge("flex flex-col", Class);
}
