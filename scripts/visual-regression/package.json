{
  "name": "lumeo-visual-regression",
  "private": true,
  "version": "1.0.0",
  "description": "Visual regression sweep of every /components/* docs route (light + dark) against committed PNG baselines.",
  "type": "module",
  "scripts": {
    "test": "node run.mjs",
    "update": "node run.mjs --update"
  },
  "dependencies": {
    "playwright-core": "^1.61.1",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0"
  }
}
