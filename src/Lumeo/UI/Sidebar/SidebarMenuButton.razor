@namespace Lumeo

@* When a Tooltip is provided we dogfood the Lumeo <Tooltip> instead of the native
   HTML title attribute. AsChild folds aria-describedby onto the focusable <a> itself
   (a wrapper div can't describe the link), and the content is only mounted while the
   rail is collapsed — an expanded rail already shows the label, so a tooltip there
   would be redundant (mirrors shadcn's sidebar tooltip semantics). *@
@if (!string.IsNullOrEmpty(Tooltip))
{
    <Tooltip Class="w-full">
        <TooltipTrigger AsChild>
            <ChildContentSlot Context="slot">
                @Anchor(slot.Attributes)
            </ChildContentSlot>
        </TooltipTrigger>
        @if (EffectiveCollapsed)
        {
            <TooltipContent Side="Lumeo.Side.Right">@Tooltip</TooltipContent>
        }
    </Tooltip>
}
else
{
    @Anchor(null)
}

@code {
    [CascadingParameter] public SidebarProvider.SidebarState? State { get; set; }
    /// <summary>Fallback label content used when <see cref="IconContent"/> is null (otherwise <see cref="IconContent"/>/<see cref="LabelContent"/> render instead).</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>Icon slot. Named <c>IconContent</c> to avoid shadowing the <c>&lt;Icon&gt;</c> component.</summary>
    [Parameter] public RenderFragment? IconContent { get; set; }

    /// <summary>Label slot. Named <c>LabelContent</c> to avoid shadowing the <c>&lt;Label&gt;</c> component.</summary>
    [Parameter] public RenderFragment? LabelContent { get; set; }

    /// <summary>The link's navigation target.</summary>
    [Parameter] public string? Href { get; set; }

    /// <summary>Hint shown via the Lumeo <c>&lt;Tooltip&gt;</c> when the rail is collapsed
    /// (icon-only). Renders as a styled, accessible tooltip — not the native browser
    /// title — and stays hidden while the rail is expanded (the label is visible then).</summary>
    [Parameter] public string? Tooltip { get; set; }

    /// <summary>Marks this button as the current page: adds <c>aria-current="page"</c> and highlights it with the accent background.</summary>
    [Parameter] public bool IsActive { get; set; }

    /// <summary>
    /// Row height: <see cref="ButtonSize.Default"/> 32px, <see cref="ButtonSize.Sm"/> 28px
    /// with 12px text, <see cref="ButtonSize.Lg"/> 48px for a two-line entry such as a team
    /// switcher or the user chip (shadcn's sidebar-07 header and footer).
    /// </summary>
    [Parameter] public ButtonSize Size { get; set; } = ButtonSize.Default;

    /// <summary><see cref="ButtonVariant.Default"/> or <see cref="ButtonVariant.Outline"/> (a bordered entry on the page background).</summary>
    [Parameter] public ButtonVariant Variant { get; set; } = ButtonVariant.Default;

    public enum ButtonSize
    {
        Default,
        Sm,
        Lg
    }

    public enum ButtonVariant
    {
        Default,
        Outline
    }

    private string SizeAttr => Size switch { ButtonSize.Sm => "sm", ButtonSize.Lg => "lg", _ => "default" };

    private string SizeClasses => Size switch
    {
        ButtonSize.Sm => "h-7 text-xs",
        ButtonSize.Lg => "h-12 text-sm group-data-[collapsible=icon]:p-0!",
        _ => "h-8 text-sm"
    };

    /// <summary>Additional CSS classes merged onto the inner <c>a</c> element.</summary>
    [Parameter] public string? Class { get; set; }

    /// <summary>Unmatched attributes are splatted onto the inner <c>a</c> element.</summary>
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    // Drives padding + label opacity. EffectiveCollapsed (provider-computed) reflects
    // hover-expansion on MiniRail too, so the label reveals while the rail is widened
    // by the cursor — not just on the pinned-toggle path. Other variants ignore the
    // hover bit so this stays identical to IsCollapsed for them.
    private bool EffectiveCollapsed => State?.EffectiveCollapsed ?? State?.IsCollapsed ?? false;

    // Shared anchor markup, rendered either bare or as the tooltip's AsChild trigger.
    // slotAttrs carries aria-describedby (only while the tooltip is open) so the link
    // itself is described to screen readers on focus.
    private RenderFragment Anchor(IReadOnlyDictionary<string, object>? slotAttrs) => Href is null
        ? @<button type="button" class="@CssClass" data-slot="sidebar-menu-button" data-sidebar="menu-button" data-size="@SizeAttr" data-active="@(IsActive ? "true" : "false")" @attributes="MergeAttrs(slotAttrs)">@Inner</button>
        : @<a href="@Href" class="@CssClass" data-slot="sidebar-menu-button" data-sidebar="menu-button" data-size="@SizeAttr" data-active="@(IsActive ? "true" : "false")" aria-current="@(IsActive ? "page" : null)" @attributes="MergeAttrs(slotAttrs)">@Inner</a>;

    private RenderFragment Inner =>
        @<text>
            @if (IconContent is not null)
            {
                <span class="shrink-0 inline-flex items-center justify-center">@IconContent</span>
                @if (LabelContent is not null)
                {
                    <span class="@LabelCssClass">@LabelContent</span>
                }
            }
            else
            {
                @ChildContent
            }
        </text>;

    private Dictionary<string, object> MergeAttrs(IReadOnlyDictionary<string, object>? slotAttrs)
    {
        var d = new Dictionary<string, object>();
        if (AdditionalAttributes is not null)
            foreach (var kv in AdditionalAttributes) d[kv.Key] = kv.Value;
        if (slotAttrs is not null)
            foreach (var kv in slotAttrs) d[kv.Key] = kv.Value; // aria-describedby (author keys already in d win only if absent here)
        return d;
    }

    private string CssClass => Cx.Merge(
        "peer/menu-button group/menu-button flex w-full items-center gap-2 rounded-md p-2 min-h-11 sm:min-h-0 text-start [&>svg]:size-4 [&>svg]:shrink-0 [&>span:last-child]:truncate text-sidebar-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground",
        SizeClasses,
        Cx.When(Variant == ButtonVariant.Outline, "bg-background shadow-[0_0_0_1px_var(--color-sidebar-border)] hover:shadow-[0_0_0_1px_var(--color-sidebar-accent)]"),
        "group-data-[collapsible=icon]:size-8! group-data-[collapsible=icon]:p-2! focus-visible:outline-none focus-visible:ring-[1.5px] focus-visible:ring-ring focus-visible:ring-offset-2 w-full overflow-hidden",
        // shadcn sidebar parity: its menu button transitions exactly
        // `transition-[width,height,padding]` with NO explicit duration/easing (Tailwind's
        // 150ms default), and deliberately does NOT transition colors — hover color changes
        // snap. The button runs on a different clock than the 200ms-linear container by
        // design; 1:1 parity means keeping that split rather than syncing them.
        "transition-[width,height,padding]",
        // Collapsed rail buttons shrink to p-2 icon squares inside the 3rem (w-12) rail —
        // shadcn's group-data-[collapsible=icon]:!p-2 on a SIDEBAR_WIDTH_ICON rail.
        Cx.When(IsActive, "bg-sidebar-accent text-sidebar-accent-foreground font-medium"),
        Class);

    // shadcn sidebar parity: NO opacity fade on the label — shadcn's menu-button text is
    // hard-CLIPPED by the collapsing width (button `overflow-hidden` +
    // `[&>span:last-child]:truncate`; the 200ms-linear width animation slides the clip
    // edge across the text). The span stays mounted in both states; visibility comes
    // entirely from geometry. Lumeo's DOM already provides the clip container (the
    // anchor's overflow-hidden above + the aside's overflow-x-hidden), so the label needs
    // no transition classes at all. The previous opacity cross-fade — even after 4.0.3
    // synced its timing to the container — was itself the remaining felt difference from
    // shadcn ("instant text reveal" vs fade), so it's removed rather than re-tuned.
    private string LabelCssClass => "whitespace-nowrap truncate";
}
