@page "/components/theme-toggle"

<PageTitle>Theme Toggle — Lumeo</PageTitle>

<PageHeader Title="Theme Toggle" Description="A simple button to toggle between light and dark mode." />

<div class="space-y-8">
    <ComponentDemo Title="Default" Code="@_defaultCode">
        <div class="flex items-center gap-4">
            <ThemeToggle />
            <span class="text-sm text-muted-foreground">Click to toggle theme</span>
        </div>
    </ComponentDemo>

    <ComponentDemo Title="In a Navbar" Code="@_navbarCode">
        <div class="flex items-center justify-between rounded-lg border bg-card px-4 py-2 w-full max-w-lg">
            <span class="font-semibold text-foreground">My App</span>
            <div class="flex items-center gap-2">
                <Button Variant="Button.ButtonVariant.Ghost" Size="Button.ButtonSize.Sm">About</Button>
                <Button Variant="Button.ButtonVariant.Ghost" Size="Button.ButtonSize.Sm">Docs</Button>
                <ThemeToggle />
            </div>
        </div>
    </ComponentDemo>
</div>

@code {
    private string _defaultCode = @"<ThemeToggle />";

    private string _navbarCode = @"<div class=""flex items-center justify-between rounded-lg border bg-card px-4 py-2"">
    <span class=""font-semibold"">My App</span>
    <div class=""flex items-center gap-2"">
        <Button Variant=""Button.ButtonVariant.Ghost"" Size=""Button.ButtonSize.Sm"">About</Button>
        <ThemeToggle />
    </div>
</div>";
}
