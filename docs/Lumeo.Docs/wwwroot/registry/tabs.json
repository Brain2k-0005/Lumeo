{
  "name": "Tabs",
  "category": "Navigation",
  "subcategory": null,
  "description": "Tabbed content with keyboard nav and animated active indicator.",
  "thumbnail": "/preview-cards/tabs.webp",
  "hasDocsPage": true,
  "nugetPackage": "Lumeo",
  "files": [
    "UI/Tabs/Tabs.razor",
    "UI/Tabs/TabsContent.razor",
    "UI/Tabs/TabsLayout.cs",
    "UI/Tabs/TabsList.razor",
    "UI/Tabs/TabsReorderEventArgs.cs",
    "UI/Tabs/TabsTrigger.razor"
  ],
  "dependencies": [],
  "packageDependencies": [
    "Blazicons.Lucide"
  ],
  "cssVars": [
    "--color-background",
    "--color-border",
    "--color-card",
    "--color-foreground",
    "--color-muted",
    "--color-muted-foreground",
    "--color-primary",
    "--color-primary-foreground",
    "--color-ring",
    "--radius"
  ],
  "api": {
    "name": "Tabs",
    "category": "Navigation",
    "subcategory": null,
    "description": "Tabbed content with keyboard nav and animated active indicator.",
    "nugetPackage": "Lumeo",
    "files": [
      "UI/Tabs/Tabs.razor",
      "UI/Tabs/TabsContent.razor",
      "UI/Tabs/TabsLayout.cs",
      "UI/Tabs/TabsList.razor",
      "UI/Tabs/TabsReorderEventArgs.cs",
      "UI/Tabs/TabsTrigger.razor"
    ],
    "namespace": "Lumeo",
    "inheritsFrom": null,
    "implements": [],
    "parameters": [
      {
        "name": "ChildContent",
        "type": "RenderFragment?",
        "default": null,
        "description": null,
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "ActiveValue",
        "type": "string",
        "default": "\"\"",
        "description": null,
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "ActiveValueChanged",
        "type": "EventCallback<string>",
        "default": null,
        "description": null,
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "Orientation",
        "type": "Lumeo.Orientation",
        "default": "Lumeo.Orientation.Horizontal",
        "description": null,
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "Variant",
        "type": "TabsVariant",
        "default": "TabsVariant.Default",
        "description": null,
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "AnimatedIndicator",
        "type": "bool",
        "default": null,
        "description": "When true, renders a sliding underline that animates between active triggers inside each TabsList. Default is false (no underline, original behaviour).",
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "RenderMode",
        "type": "TabsRenderMode",
        "default": "TabsRenderMode.Active",
        "description": "Controls when each TabsContent panel is rendered into the DOM and whether its state survives a tab switch: Active (default) — only the active panel is in the DOM; switching away unmounts it (form inputs, scroll position and any component state are lost). Lightest footprint. Lazy — a panel is rendered the first time its tab becomes active, then kept in the DOM (hidden) so its state is preserved on subsequent switches. Like DevExpress OnDemand. Eager — every panel is rendered immediately; inactive ones are hidden. State always preserved, but the heaviest initial render. Like DevExpress Always. A single TabsContent can opt into eager rendering regardless of this setting via ForceMount=\"true\".",
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "SwipeEnabled",
        "type": "bool",
        "default": null,
        "description": "When true, a horizontal pointer drag (&gt;50 px horizontal, &lt;30 px vertical) on any TabsContent panel switches to the next or previous tab. Default is false (opt-in).",
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "SwipeWrap",
        "type": "bool",
        "default": null,
        "description": "When true, swiping past the last tab wraps around to the first (and vice-versa). Default is false (no wrap-around at boundaries).",
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "Class",
        "type": "string?",
        "default": null,
        "description": null,
        "isCascading": false,
        "captureUnmatched": false
      },
      {
        "name": "AdditionalAttributes",
        "type": "Dictionary<string, object>?",
        "default": null,
        "description": null,
        "isCascading": false,
        "captureUnmatched": true
      }
    ],
    "events": [
      {
        "name": "ActiveValueChanged",
        "type": "EventCallback<string>",
        "description": null
      }
    ],
    "enums": [
      {
        "name": "TabsVariant",
        "values": [
          "Default",
          "Card",
          "Pill"
        ],
        "description": null
      },
      {
        "name": "TabsRenderMode",
        "values": [
          "Active",
          "Lazy",
          "Eager"
        ],
        "description": "Render strategy for TabsContent panels. See RenderMode for the semantics of each value."
      }
    ],
    "records": [
      {
        "name": "TabsContext",
        "signature": "TabsContext(\r\n        string ActiveValue,\r\n        EventCallback<string> SetValue,\r\n        Lumeo.Orientation Orientation,\r\n        TabsVariant Variant,\r\n        Dictionary<string, (string TabId, string PanelId)> TabIds,\r\n        bool AnimatedIndicator,\r\n        TabsRenderMode RenderMode,\r\n        HashSet<string> ActivatedValues,\r\n        bool SwipeEnabled,\r\n        bool SwipeWrap,\r\n        Action<string> RegisterContent,\r\n        Func<string, Task> SwipeNavigate,\r\n        TabsReorderState Reorder)",
        "description": null
      }
    ],
    "cssVars": [
      "--color-background",
      "--color-border",
      "--color-card",
      "--color-foreground",
      "--color-muted",
      "--color-muted-foreground",
      "--color-primary",
      "--color-primary-foreground",
      "--color-ring",
      "--radius"
    ],
    "examples": [
      {
        "title": "Default",
        "code": "<Tabs ActiveValue=\"account\">\r\n    <TabsList>\r\n        <TabsTrigger Value=\"account\">Account</TabsTrigger>\r\n        <TabsTrigger Value=\"password\">Password</TabsTrigger>\r\n    </TabsList>\r\n    <TabsContent Value=\"account\">\r\n        <Card>\r\n            <CardHeader>\r\n                <Heading Level=\"3\" Size=\"lg\" Class=\"leading-none tracking-tight\">Account</Heading>\r\n                <Lumeo.Text Size=\"sm\" Color=\"muted\">Make changes to your account here.</Lumeo.Text>\r\n            </CardHeader>\r\n            <CardContent>\r\n                <Stack Gap=\"4\">\r\n                    <FormField Label=\"Name\">\r\n                        <Input Value=\"Pedro Duarte\" />\r\n                    </FormField>\r\n                    <FormField Label=\"Username\">\r\n                        <Input Value=\"@peduarte\" />\r\n                    </FormField>\r\n                </Stack>\r\n            </CardContent>\r\n            <CardFooter>\r\n                <Button>Save changes</Button>\r\n            </CardFooter>\r\n        </Card>\r\n    </TabsContent>\r\n    <TabsContent Value=\"password\">\r\n        <Card>\r\n            <CardHeader>\r\n                <Heading Level=\"3\" Size=\"lg\" Class=\"leading-none tracking-tight\">Password</Heading>\r\n                <Lumeo.Text Size=\"sm\" Color=\"muted\">Change your password here.</Lumeo.Text>\r\n            </CardHeader>\r\n            <CardContent>\r\n                <Stack Gap=\"4\">\r\n                    <FormField Label=\"Current password\">\r\n                        <Input type=\"password\" />\r\n                    </FormField>\r\n                    <FormField Label=\"New password\">\r\n                        <Input type=\"password\" />\r\n                    </FormField>\r\n                </Stack>\r\n            </CardContent>\r\n            <CardFooter>\r\n                <Button>Save password</Button>\r\n            </CardFooter>\r\n        </Card>\r\n    </TabsContent>\r\n</Tabs>"
      },
      {
        "title": "Notifications",
        "code": "<Tabs ActiveValue=\"all\">\r\n    <TabsList>\r\n        <TabsTrigger Value=\"all\">All</TabsTrigger>\r\n        <TabsTrigger Value=\"unread\">Unread</TabsTrigger>\r\n        <TabsTrigger Value=\"archived\">Archived</TabsTrigger>\r\n    </TabsList>\r\n    <TabsContent Value=\"all\">\r\n        @foreach (var n in _notifications)\r\n        {\r\n            <Flex Gap=\"3\" Align=\"start\" Class=\"p-3 rounded-lg border\">\r\n                <span class=\"mt-1.5 h-2 w-2 rounded-full @(n.Unread ? \"bg-primary\" : \"bg-muted\")\"></span>\r\n                <Stack Gap=\"0\">\r\n                    <Lumeo.Text Size=\"sm\" Weight=\"medium\">@n.Title</Lumeo.Text>\r\n                    <Lumeo.Text Size=\"xs\" Color=\"muted\">@n.Time</Lumeo.Text>\r\n                </Stack>\r\n            </Flex>\r\n        }\r\n    </TabsContent>\r\n    <TabsContent Value=\"unread\">\r\n        @foreach (var n in _notifications.Where(x => x.Unread)) { /* same */ }\r\n    </TabsContent>\r\n    <TabsContent Value=\"archived\">\r\n        <Lumeo.Text Size=\"sm\" Color=\"muted\" Class=\"p-4 text-center\">No archived notifications.</Lumeo.Text>\r\n    </TabsContent>\r\n</Tabs>"
      },
      {
        "title": "Vertical Tabs",
        "code": "<Tabs Orientation=\"Lumeo.Orientation.Vertical\" @bind-ActiveValue=\"_verticalTab\" Class=\"flex gap-4\">\r\n    <TabsList>\r\n        <TabsTrigger Value=\"account\">Account</TabsTrigger>\r\n        <TabsTrigger Value=\"security\">Security</TabsTrigger>\r\n        <TabsTrigger Value=\"notifications\">Notifications</TabsTrigger>\r\n    </TabsList>\r\n    <div class=\"flex-1\">\r\n        <TabsContent Value=\"account\">\r\n            <Card>\r\n                <CardHeader><CardTitle>Account</CardTitle><CardDescription>Manage your account settings.</CardDescription></CardHeader>\r\n                <CardContent><Lumeo.Text Size=\"sm\" Color=\"muted\">Account settings content goes here.</Lumeo.Text></CardContent>\r\n            </Card>\r\n        </TabsContent>\r\n        <TabsContent Value=\"security\">\r\n            <Card>\r\n                <CardHeader><CardTitle>Security</CardTitle><CardDescription>Update your security preferences.</CardDescription></CardHeader>\r\n                <CardContent><Lumeo.Text Size=\"sm\" Color=\"muted\">Security settings content goes here.</Lumeo.Text></CardContent>\r\n            </Card>\r\n        </TabsContent>\r\n        <TabsContent Value=\"notifications\">\r\n            <Card>\r\n                <CardHeader><CardTitle>Notifications</CardTitle><CardDescription>Configure notification preferences.</CardDescription></CardHeader>\r\n                <CardContent><Lumeo.Text Size=\"sm\" Color=\"muted\">Notification settings content goes here.</Lumeo.Text></CardContent>\r\n            </Card>\r\n        </TabsContent>\r\n    </div>\r\n</Tabs>\r\n\r\n@code {\r\n    private string _verticalTab = \"account\";\r\n}"
      },
      {
        "title": "Simple",
        "code": "<Tabs ActiveValue=\"overview\">\r\n    <TabsList>\r\n        <TabsTrigger Value=\"overview\">Overview</TabsTrigger>\r\n        <TabsTrigger Value=\"analytics\">Analytics</TabsTrigger>\r\n        <TabsTrigger Value=\"reports\">Reports</TabsTrigger>\r\n    </TabsList>\r\n    <TabsContent Value=\"overview\">\r\n        <Lumeo.Text Size=\"sm\" Color=\"muted\" Class=\"p-4\">Overview content goes here.</Lumeo.Text>\r\n    </TabsContent>\r\n    <TabsContent Value=\"analytics\">\r\n        <Lumeo.Text Size=\"sm\" Color=\"muted\" Class=\"p-4\">Analytics content goes here.</Lumeo.Text>\r\n    </TabsContent>\r\n    <TabsContent Value=\"reports\">\r\n        <Lumeo.Text Size=\"sm\" Color=\"muted\" Class=\"p-4\">Reports content goes here.</Lumeo.Text>\r\n    </TabsContent>\r\n</Tabs>"
      }
    ],
    "subComponents": {
      "TabsContent": {
        "componentName": "TabsContent",
        "fileName": "TabsContent.razor",
        "namespace": "Lumeo",
        "inheritsFrom": null,
        "implements": [
          "IAsyncDisposable"
        ],
        "parameters": [
          {
            "name": "Context",
            "type": "Tabs.TabsContext",
            "default": "default!",
            "description": null,
            "isCascading": true,
            "captureUnmatched": false
          },
          {
            "name": "ChildContent",
            "type": "RenderFragment?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "Value",
            "type": "string",
            "default": "\"\"",
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "ForceMount",
            "type": "bool",
            "default": null,
            "description": "When true, this panel is always rendered into the DOM (hidden when inactive), regardless of the parent RenderMode. Use for panels whose state must survive every tab switch — e.g. a half-filled form — while leaving the rest of the tab set on the lighter default mode.",
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "Class",
            "type": "string?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "AdditionalAttributes",
            "type": "Dictionary<string, object>?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": true
          }
        ],
        "events": [],
        "enums": [],
        "records": [],
        "parseFailed": false,
        "parseError": null
      },
      "TabsList": {
        "componentName": "TabsList",
        "fileName": "TabsList.razor",
        "namespace": "Lumeo",
        "inheritsFrom": null,
        "implements": [
          "IAsyncDisposable"
        ],
        "parameters": [
          {
            "name": "Context",
            "type": "Tabs.TabsContext?",
            "default": null,
            "description": null,
            "isCascading": true,
            "captureUnmatched": false
          },
          {
            "name": "ChildContent",
            "type": "RenderFragment?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "TrailingContent",
            "type": "RenderFragment?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "Scrollable",
            "type": "bool",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "Reorderable",
            "type": "bool",
            "default": null,
            "description": "When true, each enabled TabsTrigger becomes draggable (HTML5 drag-and-drop on desktop, touch drag on mobile). On a successful reorder the library raises OnReorder with the from/to indices and moved value — the library does NOT mutate the order. The consumer's handler is responsible for updating the underlying collection and re-rendering.",
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "OnReorder",
            "type": "EventCallback<TabsReorderEventArgs>",
            "default": null,
            "description": "Fires when the user drops a dragged TabsTrigger on a new position. Only meaningful when Reorderable is true.",
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "SavedLayout",
            "type": "TabsLayout?",
            "default": null,
            "description": "Optional previously-persisted tab ordering. The library does not mutate the consumer's backing collection on its own — pass this to your parent component on init and apply Order to your collection before rendering. Mirrors DataGrid's SavedLayout parameter and pairs with OnLayoutChanged to round-trip the order through your storage of choice.",
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "OnLayoutChanged",
            "type": "EventCallback<TabsLayout>",
            "default": null,
            "description": "Fires immediately after each successful reorder with the post-drop tab order, so the consumer can persist it (e.g. to local storage). The snapshot reflects the order the consumer's collection will have once it applies the corresponding OnReorder. Only meaningful when Reorderable is true. Pairs with SavedLayout to round-trip the order through storage.",
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "Class",
            "type": "string?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "AdditionalAttributes",
            "type": "Dictionary<string, object>?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": true
          }
        ],
        "events": [
          {
            "name": "OnReorder",
            "type": "EventCallback<TabsReorderEventArgs>",
            "description": "Fires when the user drops a dragged TabsTrigger on a new position. Only meaningful when Reorderable is true."
          },
          {
            "name": "OnLayoutChanged",
            "type": "EventCallback<TabsLayout>",
            "description": "Fires immediately after each successful reorder with the post-drop tab order, so the consumer can persist it (e.g. to local storage). The snapshot reflects the order the consumer's collection will have once it applies the corresponding OnReorder. Only meaningful when Reorderable is true. Pairs with SavedLayout to round-trip the order through storage."
          }
        ],
        "enums": [],
        "records": [],
        "parseFailed": false,
        "parseError": null
      },
      "TabsTrigger": {
        "componentName": "TabsTrigger",
        "fileName": "TabsTrigger.razor",
        "namespace": "Lumeo",
        "inheritsFrom": null,
        "implements": [],
        "parameters": [
          {
            "name": "Context",
            "type": "Tabs.TabsContext",
            "default": "default!",
            "description": null,
            "isCascading": true,
            "captureUnmatched": false
          },
          {
            "name": "ChildContent",
            "type": "RenderFragment?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "IconContent",
            "type": "RenderFragment?",
            "default": null,
            "description": "Icon slot. Named IconContent to avoid shadowing the &lt;Icon&gt; component.",
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "TrailingContent",
            "type": "RenderFragment?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "Value",
            "type": "string",
            "default": "\"\"",
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "IsClosable",
            "type": "bool",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "Disabled",
            "type": "bool",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "OnClose",
            "type": "EventCallback",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "Class",
            "type": "string?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": false
          },
          {
            "name": "AdditionalAttributes",
            "type": "Dictionary<string, object>?",
            "default": null,
            "description": null,
            "isCascading": false,
            "captureUnmatched": true
          }
        ],
        "events": [
          {
            "name": "OnClose",
            "type": "EventCallback",
            "description": null
          }
        ],
        "enums": [],
        "records": [],
        "parseFailed": false,
        "parseError": null
      }
    },
    "parseFailed": false,
    "parseError": null
  },
  "$schema": "https://lumeo.nativ.sh/registry-component-schema.json",
  "key": "tabs",
  "docsUrl": "https://lumeo.nativ.sh/components/tabs",
  "sourceUrl": [
    {
      "file": "UI/Tabs/Tabs.razor",
      "url": "https://github.com/Brain2k-0005/Lumeo/tree/master/src/Lumeo/UI/Tabs/Tabs.razor"
    },
    {
      "file": "UI/Tabs/TabsContent.razor",
      "url": "https://github.com/Brain2k-0005/Lumeo/tree/master/src/Lumeo/UI/Tabs/TabsContent.razor"
    },
    {
      "file": "UI/Tabs/TabsLayout.cs",
      "url": "https://github.com/Brain2k-0005/Lumeo/tree/master/src/Lumeo/UI/Tabs/TabsLayout.cs"
    },
    {
      "file": "UI/Tabs/TabsList.razor",
      "url": "https://github.com/Brain2k-0005/Lumeo/tree/master/src/Lumeo/UI/Tabs/TabsList.razor"
    },
    {
      "file": "UI/Tabs/TabsReorderEventArgs.cs",
      "url": "https://github.com/Brain2k-0005/Lumeo/tree/master/src/Lumeo/UI/Tabs/TabsReorderEventArgs.cs"
    },
    {
      "file": "UI/Tabs/TabsTrigger.razor",
      "url": "https://github.com/Brain2k-0005/Lumeo/tree/master/src/Lumeo/UI/Tabs/TabsTrigger.razor"
    }
  ],
  "source": [
    {
      "path": "UI/Tabs/Tabs.razor",
      "content": "@namespace Lumeo\r\n\r\n@{\r\n    var context = new TabsContext(ActiveValue, EventCallback.Factory.Create<string>(this, SetValue), Orientation, Variant, _tabIds, AnimatedIndicator, RenderMode, _activatedValues, SwipeEnabled, SwipeWrap, RegisterContent, NavigateBySwipe, _reorderState);\r\n}\r\n\r\n<div class=\"@CssClass\" @attributes=\"AdditionalAttributes\">\r\n    <CascadingValue Value=\"context\" IsFixed=\"false\">\r\n        @ChildContent\r\n    </CascadingValue>\r\n</div>\r\n\r\n@code {\r\n    [Parameter] public RenderFragment? ChildContent { get; set; }\r\n    [Parameter] public string ActiveValue { get; set; } = \"\";\r\n    [Parameter] public EventCallback<string> ActiveValueChanged { get; set; }\r\n    [Parameter] public Lumeo.Orientation Orientation { get; set; } = Lumeo.Orientation.Horizontal;\r\n    [Parameter] public TabsVariant Variant { get; set; } = TabsVariant.Default;\r\n\r\n    /// <summary>\r\n    /// When true, renders a sliding underline that animates between active\r\n    /// triggers inside each <see cref=\"TabsList\"/>. Default is <c>false</c>\r\n    /// (no underline, original behaviour).\r\n    /// </summary>\r\n    [Parameter] public bool AnimatedIndicator { get; set; }\r\n\r\n    /// <summary>\r\n    /// Controls when each <see cref=\"TabsContent\"/> panel is rendered into\r\n    /// the DOM and whether its state survives a tab switch:\r\n    /// <list type=\"bullet\">\r\n    /// <item><see cref=\"TabsRenderMode.Active\"/> (default) — only the active\r\n    /// panel is in the DOM; switching away unmounts it (form inputs, scroll\r\n    /// position and any component state are lost). Lightest footprint.</item>\r\n    /// <item><see cref=\"TabsRenderMode.Lazy\"/> — a panel is rendered the\r\n    /// first time its tab becomes active, then kept in the DOM (hidden) so\r\n    /// its state is preserved on subsequent switches. Like DevExpress\r\n    /// <c>OnDemand</c>.</item>\r\n    /// <item><see cref=\"TabsRenderMode.Eager\"/> — every panel is rendered\r\n    /// immediately; inactive ones are hidden. State always preserved, but\r\n    /// the heaviest initial render. Like DevExpress <c>Always</c>.</item>\r\n    /// </list>\r\n    /// A single <see cref=\"TabsContent\"/> can opt into eager rendering\r\n    /// regardless of this setting via <c>ForceMount=\"true\"</c>.\r\n    /// </summary>\r\n    [Parameter] public TabsRenderMode RenderMode { get; set; } = TabsRenderMode.Active;\r\n\r\n    /// <summary>\r\n    /// When true, a horizontal pointer drag (&gt;50 px horizontal, &lt;30 px\r\n    /// vertical) on any <see cref=\"TabsContent\"/> panel switches to the\r\n    /// next or previous tab. Default is <c>false</c> (opt-in).\r\n    /// </summary>\r\n    [Parameter] public bool SwipeEnabled { get; set; }\r\n\r\n    /// <summary>\r\n    /// When true, swiping past the last tab wraps around to the first (and\r\n    /// vice-versa). Default is <c>false</c> (no wrap-around at boundaries).\r\n    /// </summary>\r\n    [Parameter] public bool SwipeWrap { get; set; }\r\n\r\n    [Parameter] public string? Class { get; set; }\r\n    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }\r\n\r\n    // Registry: value -> (tabId, panelId) — populated by TabsTrigger on first render\r\n    private readonly Dictionary<string, (string TabId, string PanelId)> _tabIds = new();\r\n    // Set of tab values that have ever been the active tab — drives Lazy mode.\r\n    private readonly HashSet<string> _activatedValues = new();\r\n    // Ordered list of registered TabsContent values — used for swipe navigation.\r\n    private readonly List<string> _contentOrder = new();\r\n    // Shared mutable state for TabsList reordering — owned by TabsList but\r\n    // referenced via the cascading context so triggers can read it.\r\n    private readonly TabsReorderState _reorderState = new();\r\n\r\n    private string CssClass => Orientation == Lumeo.Orientation.Vertical\r\n        ? $\"flex flex-row {Class}\".Trim()\r\n        : $\"{Class}\".Trim();\r\n\r\n    protected override void OnParametersSet()\r\n    {\r\n        // Track activations so Lazy mode knows which panels to keep mounted.\r\n        if (!string.IsNullOrEmpty(ActiveValue))\r\n            _activatedValues.Add(ActiveValue);\r\n    }\r\n\r\n    internal void RegisterContent(string value)\r\n    {\r\n        if (!_contentOrder.Contains(value))\r\n            _contentOrder.Add(value);\r\n    }\r\n\r\n    internal async Task NavigateBySwipe(string direction)\r\n    {\r\n        if (_contentOrder.Count == 0) return;\r\n        var idx = _contentOrder.IndexOf(ActiveValue);\r\n        if (idx < 0) return;\r\n\r\n        int next;\r\n        if (direction == \"next\")\r\n        {\r\n            next = idx + 1;\r\n            if (next >= _contentOrder.Count)\r\n            {\r\n                if (!SwipeWrap) return;\r\n                next = 0;\r\n            }\r\n        }\r\n        else // \"prev\"\r\n        {\r\n            next = idx - 1;\r\n            if (next < 0)\r\n            {\r\n                if (!SwipeWrap) return;\r\n                next = _contentOrder.Count - 1;\r\n            }\r\n        }\r\n\r\n        await SetValue(_contentOrder[next]);\r\n        StateHasChanged();\r\n    }\r\n\r\n    private async Task SetValue(string value)\r\n    {\r\n        ActiveValue = value;\r\n        if (!string.IsNullOrEmpty(value))\r\n            _activatedValues.Add(value);\r\n        await ActiveValueChanged.InvokeAsync(value);\r\n    }\r\n\r\n    public record TabsContext(\r\n        string ActiveValue,\r\n        EventCallback<string> SetValue,\r\n        Lumeo.Orientation Orientation,\r\n        TabsVariant Variant,\r\n        Dictionary<string, (string TabId, string PanelId)> TabIds,\r\n        bool AnimatedIndicator,\r\n        TabsRenderMode RenderMode,\r\n        HashSet<string> ActivatedValues,\r\n        bool SwipeEnabled,\r\n        bool SwipeWrap,\r\n        Action<string> RegisterContent,\r\n        Func<string, Task> SwipeNavigate,\r\n        TabsReorderState Reorder)\r\n    {\r\n        public TabsContext(string ActiveValue, EventCallback<string> SetValue, Lumeo.Orientation Orientation = Lumeo.Orientation.Horizontal, TabsVariant Variant = TabsVariant.Default)\r\n            : this(ActiveValue, SetValue, Orientation, Variant, new Dictionary<string, (string, string)>(), false, TabsRenderMode.Active, new HashSet<string>(), false, false, _ => { }, _ => Task.CompletedTask, new TabsReorderState()) { }\r\n    }\r\n\r\n    /// <summary>\r\n    /// Shared mutable state that flows from <see cref=\"TabsList\"/> down to its\r\n    /// <see cref=\"TabsTrigger\"/> children via the cascading <see cref=\"TabsContext\"/>.\r\n    /// Carries the per-render ordered list of trigger values, the\r\n    /// currently-dragged value (for opacity feedback), and the callbacks that\r\n    /// triggers fire on drag-start / drop. Only meaningful when the enclosing\r\n    /// <see cref=\"TabsList\"/> has <c>Reorderable=\"true\"</c>.\r\n    /// </summary>\r\n    public sealed class TabsReorderState\r\n    {\r\n        public bool Enabled { get; set; }\r\n        public string? DraggingValue { get; set; }\r\n        public List<string> Order { get; } = new();\r\n        public Action<string>? OnDragStart { get; set; }\r\n        public Func<string, Task>? OnDrop { get; set; }\r\n        public Action? OnDragEnd { get; set; }\r\n        public Action<string>? OnDragOver { get; set; }\r\n        public string? DragOverValue { get; set; }\r\n    }\r\n\r\n    public enum TabsVariant\r\n    {\r\n        Default,\r\n        Card,\r\n        Pill\r\n    }\r\n\r\n    /// <summary>Render strategy for <see cref=\"TabsContent\"/> panels. See\r\n    /// <see cref=\"Tabs.RenderMode\"/> for the semantics of each value.</summary>\r\n    public enum TabsRenderMode\r\n    {\r\n        /// <summary>Only the active panel is in the DOM; switching away unmounts it. Default.</summary>\r\n        Active,\r\n        /// <summary>Panels render on first activation and stay mounted (hidden) afterwards.</summary>\r\n        Lazy,\r\n        /// <summary>All panels render immediately; inactive ones are hidden.</summary>\r\n        Eager\r\n    }\r\n}\r\n"
    },
    {
      "path": "UI/Tabs/TabsContent.razor",
      "content": "@namespace Lumeo\r\n@inject Lumeo.Services.IComponentInteropService Interop\r\n@inject Microsoft.Extensions.Options.IOptions<Lumeo.Services.LumeoGestureOptions> GestureOpts\r\n@implements IAsyncDisposable\r\n\r\n@if (ShouldRender_)\r\n{\r\n    <div role=\"tabpanel\"\r\n         id=\"@PanelId\"\r\n         aria-labelledby=\"@TabId\"\r\n         tabindex=\"0\"\r\n         hidden=\"@(IsActive ? null : \"hidden\")\"\r\n         class=\"@CssClass\"\r\n         @attributes=\"AdditionalAttributes\">\r\n        @ChildContent\r\n    </div>\r\n}\r\n\r\n@code {\r\n    [CascadingParameter] public Tabs.TabsContext Context { get; set; } = default!;\r\n    [Parameter] public RenderFragment? ChildContent { get; set; }\r\n    [Parameter, EditorRequired] public string Value { get; set; } = \"\";\r\n\r\n    /// <summary>\r\n    /// When true, this panel is always rendered into the DOM (hidden when\r\n    /// inactive), regardless of the parent <see cref=\"Tabs.RenderMode\"/>.\r\n    /// Use for panels whose state must survive every tab switch — e.g. a\r\n    /// half-filled form — while leaving the rest of the tab set on the\r\n    /// lighter default mode.\r\n    /// </summary>\r\n    [Parameter] public bool ForceMount { get; set; }\r\n\r\n    [Parameter] public string? Class { get; set; }\r\n    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }\r\n\r\n    private bool IsActive => Context.ActiveValue == Value;\r\n    private bool _swipeAttached;\r\n\r\n    // Decide whether this panel's <div> exists in the DOM at all.\r\n    //   Active mode  → only when this tab is the active one\r\n    //   Lazy mode    → once this tab has ever been activated (or ForceMount)\r\n    //   Eager mode   → always (or ForceMount)\r\n    private bool ShouldRender_\r\n    {\r\n        get\r\n        {\r\n            if (ForceMount) return true;\r\n            return Context.RenderMode switch\r\n            {\r\n                Tabs.TabsRenderMode.Eager => true,\r\n                Tabs.TabsRenderMode.Lazy  => IsActive || Context.ActivatedValues.Contains(Value),\r\n                _                          => IsActive\r\n            };\r\n        }\r\n    }\r\n\r\n    private string PanelId => Context.TabIds.TryGetValue(Value, out var ids) ? ids.PanelId : $\"tabpanel-{Value}\";\r\n    private string TabId => Context.TabIds.TryGetValue(Value, out var ids) ? ids.TabId : $\"tab-{Value}\";\r\n\r\n    private string CssClass => Context.SwipeEnabled\r\n        ? $\"mt-2 focus-visible:outline-none touch-pan-y {Class}\".Trim()\r\n        : $\"mt-2 focus-visible:outline-none {Class}\".Trim();\r\n\r\n    protected override void OnInitialized()\r\n    {\r\n        // Register this panel's value in the parent's ordered list.\r\n        Context.RegisterContent(Value);\r\n    }\r\n\r\n    protected override async Task OnAfterRenderAsync(bool firstRender)\r\n    {\r\n        if (!Context.SwipeEnabled) return;\r\n\r\n        // Re-attach whenever this panel transitions from inactive → active.\r\n        // In Active render mode the <div> is removed from the DOM when the tab\r\n        // becomes inactive, so every time it reappears it is a fresh element\r\n        // with no JS listeners — we must re-register each time.\r\n        var shouldAttach = IsActive;\r\n        if (shouldAttach && !_swipeAttached)\r\n        {\r\n            _swipeAttached = true;\r\n            try\r\n            {\r\n                await Interop.RegisterTabSwipe(PanelId, Context.SwipeWrap, async direction =>\r\n                {\r\n                    // Navigate via the parent Tabs component's method through the context callback.\r\n                    // The context carries a delegate that resolves to Tabs.NavigateBySwipe.\r\n                    await Context.SwipeNavigate(direction);\r\n                }, GestureOpts.Value.SwipeThresholdPx, GestureOpts.Value.VerticalDeadZonePx);\r\n            }\r\n            catch (JSDisconnectedException) { _swipeAttached = false; }\r\n            catch (InvalidOperationException) { _swipeAttached = false; } // JS unavailable during prerender\r\n        }\r\n        else if (!shouldAttach && _swipeAttached)\r\n        {\r\n            _swipeAttached = false;\r\n            try { await Interop.UnregisterTabSwipe(PanelId); }\r\n            catch (JSDisconnectedException) { }\r\n        }\r\n    }\r\n\r\n    public async ValueTask DisposeAsync()\r\n    {\r\n        if (!_swipeAttached) return;\r\n        try\r\n        {\r\n            await Interop.UnregisterTabSwipe(PanelId);\r\n        }\r\n        catch (JSDisconnectedException) { }\r\n    }\r\n}\r\n"
    },
    {
      "path": "UI/Tabs/TabsLayout.cs",
      "content": "namespace Lumeo;\r\n\r\n/// <summary>\r\n/// JSON-serializable snapshot of a <see cref=\"TabsList\"/>'s current tab order.\r\n/// Used to persist user-reordered tabs across sessions in tandem with\r\n/// <see cref=\"TabsList.SavedLayout\"/> and <see cref=\"TabsList.OnLayoutChanged\"/>.\r\n/// </summary>\r\n/// <param name=\"Order\">\r\n/// The ordered list of <see cref=\"TabsTrigger.Value\"/> identifiers, in the\r\n/// order they should appear in the list.\r\n/// </param>\r\n/// <remarks>\r\n/// The library never mutates the consumer's backing collection. The consumer\r\n/// is responsible for:\r\n/// <list type=\"bullet\">\r\n/// <item>Persisting the <see cref=\"TabsLayout\"/> received from\r\n/// <see cref=\"TabsList.OnLayoutChanged\"/> (e.g. to local storage or a server).</item>\r\n/// <item>Re-ordering its backing collection from <see cref=\"Order\"/> on\r\n/// initialisation, before passing it to <see cref=\"TabsList.SavedLayout\"/>.</item>\r\n/// </list>\r\n/// This mirrors the existing reorder model where <c>OnReorder</c> reports the\r\n/// drag without applying it — the consumer remains the source of truth.\r\n/// </remarks>\r\npublic sealed record TabsLayout(IReadOnlyList<string> Order);\r\n"
    },
    {
      "path": "UI/Tabs/TabsList.razor",
      "content": "@namespace Lumeo\r\n@inject Lumeo.Services.IComponentInteropService Interop\r\n@implements IAsyncDisposable\r\n\r\n@{\r\n    // When reorderable, wire the shared TabsReorderState that flows to each\r\n    // TabsTrigger via the cascading TabsContext, and reset the per-render\r\n    // ordered list so triggers can append themselves in render order.\r\n    if (Context is not null)\r\n    {\r\n        Context.Reorder.Enabled = Reorderable;\r\n        if (Reorderable)\r\n        {\r\n            Context.Reorder.Order.Clear();\r\n            Context.Reorder.OnDragStart = HandleDragStart;\r\n            Context.Reorder.OnDrop = HandleDrop;\r\n            Context.Reorder.OnDragEnd = HandleDragEnd;\r\n            Context.Reorder.OnDragOver = HandleDragOver;\r\n        }\r\n        else\r\n        {\r\n            Context.Reorder.DraggingValue = null;\r\n            Context.Reorder.DragOverValue = null;\r\n        }\r\n    }\r\n}\r\n\r\n@if (Scrollable)\r\n{\r\n    <div class=\"overflow-hidden\">\r\n        <div id=\"@_listId\" role=\"tablist\" aria-orientation=\"@OrientationStr\" class=\"@CssClass overflow-x-auto scrollbar-none\" style=\"@IndicatorStyle\" data-sliding-pill=\"@(UsePillIndicator ? \"true\" : null)\" @attributes=\"AdditionalAttributes\">\r\n            @ChildContent\r\n            @if (TrailingContent is not null)\r\n            {\r\n                @TrailingContent\r\n            }\r\n            @if (RenderIndicator)\r\n            {\r\n                <span aria-hidden=\"true\" class=\"@IndicatorClass\" data-ready=\"@(_indicatorReady ? \"true\" : \"false\")\"></span>\r\n            }\r\n        </div>\r\n    </div>\r\n}\r\nelse\r\n{\r\n    <div id=\"@_listId\" role=\"tablist\" aria-orientation=\"@OrientationStr\" class=\"@CssClass\" style=\"@IndicatorStyle\" data-sliding-pill=\"@(UsePillIndicator ? \"true\" : null)\" @attributes=\"AdditionalAttributes\">\r\n        @ChildContent\r\n        @if (TrailingContent is not null)\r\n        {\r\n            @TrailingContent\r\n        }\r\n        @if (RenderIndicator)\r\n        {\r\n            <span aria-hidden=\"true\" class=\"@IndicatorClass\" data-ready=\"@(_indicatorReady ? \"true\" : \"false\")\"></span>\r\n        }\r\n    </div>\r\n}\r\n\r\n@code {\r\n    [CascadingParameter] public Tabs.TabsContext? Context { get; set; }\r\n\r\n    [Parameter] public RenderFragment? ChildContent { get; set; }\r\n    [Parameter] public RenderFragment? TrailingContent { get; set; }\r\n    [Parameter] public bool Scrollable { get; set; }\r\n\r\n    /// <summary>\r\n    /// When true, each enabled <see cref=\"TabsTrigger\"/> becomes draggable\r\n    /// (HTML5 drag-and-drop on desktop, touch drag on mobile). On a successful\r\n    /// reorder the library raises <see cref=\"OnReorder\"/> with the from/to\r\n    /// indices and moved value — <b>the library does NOT mutate the order</b>.\r\n    /// The consumer's handler is responsible for updating the underlying\r\n    /// collection and re-rendering.\r\n    /// </summary>\r\n    /// <example>\r\n    /// <code>\r\n    /// &lt;TabsList Reorderable=\"true\" OnReorder=\"HandleReorder\"&gt;\r\n    ///     @foreach (var t in _tabs)\r\n    ///     {\r\n    ///         &lt;TabsTrigger @key=\"t.Value\" Value=\"@t.Value\"&gt;@t.Label&lt;/TabsTrigger&gt;\r\n    ///     }\r\n    /// &lt;/TabsList&gt;\r\n    ///\r\n    /// @code {\r\n    ///     private void HandleReorder(TabsReorderEventArgs e)\r\n    ///     {\r\n    ///         var moved = _tabs[e.FromIndex];\r\n    ///         _tabs.RemoveAt(e.FromIndex);\r\n    ///         _tabs.Insert(e.ToIndex, moved);\r\n    ///     }\r\n    /// }\r\n    /// </code>\r\n    /// </example>\r\n    [Parameter] public bool Reorderable { get; set; }\r\n\r\n    /// <summary>\r\n    /// Fires when the user drops a dragged <see cref=\"TabsTrigger\"/> on a new\r\n    /// position. Only meaningful when <see cref=\"Reorderable\"/> is true.\r\n    /// </summary>\r\n    [Parameter] public EventCallback<TabsReorderEventArgs> OnReorder { get; set; }\r\n\r\n    /// <summary>\r\n    /// Optional previously-persisted tab ordering. The library does not mutate\r\n    /// the consumer's backing collection on its own — pass this to your parent\r\n    /// component on init and apply <see cref=\"TabsLayout.Order\"/> to your\r\n    /// collection before rendering. Mirrors DataGrid's <c>SavedLayout</c>\r\n    /// parameter and pairs with <see cref=\"OnLayoutChanged\"/> to round-trip\r\n    /// the order through your storage of choice.\r\n    /// </summary>\r\n    /// <example>\r\n    /// <code>\r\n    /// // On init: read the persisted layout and re-order the backing list.\r\n    /// protected override void OnInitialized()\r\n    /// {\r\n    ///     _savedLayout = LoadFromStorage();\r\n    ///     if (_savedLayout is not null)\r\n    ///         _tabs = _tabs.OrderBy(t => _savedLayout.Order.IndexOf(t.Value)).ToList();\r\n    /// }\r\n    ///\r\n    /// // On change: persist the new layout so it survives the next load.\r\n    /// private Task HandleLayoutChanged(TabsLayout layout)\r\n    /// {\r\n    ///     _savedLayout = layout;\r\n    ///     return SaveToStorage(layout);\r\n    /// }\r\n    /// </code>\r\n    /// </example>\r\n    [Parameter] public TabsLayout? SavedLayout { get; set; }\r\n\r\n    /// <summary>\r\n    /// Fires immediately after each successful reorder with the post-drop tab\r\n    /// order, so the consumer can persist it (e.g. to local storage). The\r\n    /// snapshot reflects the order the consumer's collection will have once\r\n    /// it applies the corresponding <see cref=\"OnReorder\"/>. Only meaningful\r\n    /// when <see cref=\"Reorderable\"/> is true. Pairs with\r\n    /// <see cref=\"SavedLayout\"/> to round-trip the order through storage.\r\n    /// </summary>\r\n    [Parameter] public EventCallback<TabsLayout> OnLayoutChanged { get; set; }\r\n\r\n    [Parameter] public string? Class { get; set; }\r\n    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }\r\n\r\n    private readonly string _listId = LumeoIds.New(\"tabslist\");\r\n    private double _indicatorX;\r\n    private double _indicatorW;\r\n    private bool _indicatorReady;\r\n    private string? _lastMeasuredValue;\r\n    private bool _touchRegistered;\r\n\r\n    private bool IsVertical => Context?.Orientation == Lumeo.Orientation.Vertical;\r\n    private string OrientationStr => IsVertical ? \"vertical\" : \"horizontal\";\r\n    private Tabs.TabsVariant CurrentVariant => Context?.Variant ?? Tabs.TabsVariant.Default;\r\n\r\n    private bool RenderIndicator => Context?.AnimatedIndicator == true && !IsVertical;\r\n\r\n    // Default and Pill variants render a background pill that slides; Card uses an underline.\r\n    private bool UsePillIndicator => RenderIndicator && CurrentVariant != Tabs.TabsVariant.Card;\r\n\r\n    private string IndicatorClass => UsePillIndicator ? \"lumeo-tabs-indicator\" : \"lumeo-tabs-underline\";\r\n\r\n    private string? IndicatorStyle => RenderIndicator\r\n        ? $\"--lumeo-tabs-indicator-x: {_indicatorX.ToString(System.Globalization.CultureInfo.InvariantCulture)}px; --lumeo-tabs-indicator-w: {_indicatorW.ToString(System.Globalization.CultureInfo.InvariantCulture)}px;\"\r\n        : null;\r\n\r\n    protected override async Task OnAfterRenderAsync(bool firstRender)\r\n    {\r\n        if (Reorderable && firstRender && !_touchRegistered)\r\n        {\r\n            try\r\n            {\r\n                await Interop.RegisterSortableTouch(_listId, HandleTouchDrop);\r\n                _touchRegistered = true;\r\n            }\r\n            catch (JSDisconnectedException) { }\r\n            catch (InvalidOperationException) { } // prerender\r\n        }\r\n\r\n        if (!RenderIndicator || Context is null) return;\r\n\r\n        var active = Context.ActiveValue;\r\n        if (string.IsNullOrEmpty(active)) return;\r\n        if (!Context.TabIds.TryGetValue(active, out var ids)) return;\r\n        if (_lastMeasuredValue == active && _indicatorReady) return;\r\n\r\n        ComponentInteropService.TabMeasurement? measurement;\r\n        try\r\n        {\r\n            measurement = await Interop.TabsMeasure(ids.TabId);\r\n        }\r\n        catch (JSDisconnectedException) { return; }\r\n        catch (InvalidOperationException) { return; } // thrown during prerender when JS is unavailable\r\n        if (measurement is null) return;\r\n\r\n        var changed = Math.Abs(measurement.X - _indicatorX) > 0.5\r\n                     || Math.Abs(measurement.Width - _indicatorW) > 0.5\r\n                     || !_indicatorReady;\r\n\r\n        _indicatorX = measurement.X;\r\n        _indicatorW = measurement.Width;\r\n        _indicatorReady = true;\r\n        _lastMeasuredValue = active;\r\n\r\n        if (changed)\r\n        {\r\n            StateHasChanged();\r\n        }\r\n    }\r\n\r\n    public async ValueTask DisposeAsync()\r\n    {\r\n        if (_touchRegistered)\r\n        {\r\n            await Interop.UnregisterSortableTouch(_listId);\r\n            _touchRegistered = false;\r\n        }\r\n    }\r\n\r\n    private void HandleDragStart(string value)\r\n    {\r\n        if (Context is null) return;\r\n        Context.Reorder.DraggingValue = value;\r\n        StateHasChanged();\r\n    }\r\n\r\n    private void HandleDragOver(string value)\r\n    {\r\n        if (Context is null) return;\r\n        if (Context.Reorder.DragOverValue == value) return;\r\n        Context.Reorder.DragOverValue = value;\r\n        StateHasChanged();\r\n    }\r\n\r\n    private void HandleDragEnd()\r\n    {\r\n        if (Context is null) return;\r\n        Context.Reorder.DraggingValue = null;\r\n        Context.Reorder.DragOverValue = null;\r\n        StateHasChanged();\r\n    }\r\n\r\n    private async Task HandleDrop(string targetValue)\r\n    {\r\n        if (Context is null) return;\r\n        var from = Context.Reorder.DraggingValue;\r\n        Context.Reorder.DraggingValue = null;\r\n        Context.Reorder.DragOverValue = null;\r\n\r\n        if (string.IsNullOrEmpty(from) || from == targetValue)\r\n        {\r\n            StateHasChanged();\r\n            return;\r\n        }\r\n\r\n        var order = Context.Reorder.Order;\r\n        var fromIndex = order.IndexOf(from);\r\n        var toIndex = order.IndexOf(targetValue);\r\n        if (fromIndex < 0 || toIndex < 0)\r\n        {\r\n            StateHasChanged();\r\n            return;\r\n        }\r\n\r\n        StateHasChanged();\r\n        await OnReorder.InvokeAsync(new TabsReorderEventArgs(fromIndex, toIndex, from));\r\n        await NotifyLayoutChangedAsync(fromIndex, toIndex);\r\n    }\r\n\r\n    private async Task HandleTouchDrop(int source, int target)\r\n    {\r\n        if (Context is null) return;\r\n        var order = Context.Reorder.Order;\r\n        if (source < 0 || target < 0 || source >= order.Count || target >= order.Count || source == target) return;\r\n        var moved = order[source];\r\n        await InvokeAsync(async () =>\r\n        {\r\n            await OnReorder.InvokeAsync(new TabsReorderEventArgs(source, target, moved));\r\n            await NotifyLayoutChangedAsync(source, target);\r\n        });\r\n    }\r\n\r\n    // Computes the post-drop tab order (without mutating the live Order list,\r\n    // which is re-populated per-render by TabsTrigger) and raises\r\n    // OnLayoutChanged so the consumer can persist it.\r\n    private async Task NotifyLayoutChangedAsync(int fromIndex, int toIndex)\r\n    {\r\n        if (!OnLayoutChanged.HasDelegate || Context is null) return;\r\n        var live = Context.Reorder.Order;\r\n        if (fromIndex < 0 || toIndex < 0 || fromIndex >= live.Count || toIndex >= live.Count) return;\r\n\r\n        var snapshot = new List<string>(live);\r\n        var moved = snapshot[fromIndex];\r\n        snapshot.RemoveAt(fromIndex);\r\n        snapshot.Insert(toIndex, moved);\r\n        await OnLayoutChanged.InvokeAsync(new TabsLayout(snapshot));\r\n    }\r\n\r\n    private string CssClass\r\n    {\r\n        get\r\n        {\r\n            var baseClasses = CurrentVariant switch\r\n            {\r\n                Tabs.TabsVariant.Card => IsVertical\r\n                    ? \"flex flex-col h-auto w-auto items-stretch justify-start border-r border-border/40 text-muted-foreground\"\r\n                    : \"inline-flex h-10 items-center justify-center border-b border-border/40 text-muted-foreground\",\r\n                Tabs.TabsVariant.Pill => IsVertical\r\n                    ? \"flex flex-col h-auto w-auto items-stretch justify-start gap-1 text-muted-foreground\"\r\n                    : \"inline-flex h-10 items-center justify-center gap-1 text-muted-foreground\",\r\n                _ => IsVertical\r\n                    ? \"flex flex-col h-auto w-auto items-stretch justify-start rounded-lg bg-muted p-1 text-muted-foreground\"\r\n                    : \"inline-flex h-9 items-center justify-center rounded-lg bg-muted p-1 text-muted-foreground\"\r\n            };\r\n            var indicatorWrapper = RenderIndicator ? \"relative lumeo-tabs-list\" : null;\r\n            return Cx.Join(baseClasses, indicatorWrapper, Class);\r\n        }\r\n    }\r\n}\r\n"
    },
    {
      "path": "UI/Tabs/TabsReorderEventArgs.cs",
      "content": "namespace Lumeo;\r\n\r\n/// <summary>\r\n/// Payload for <see cref=\"TabsList.OnReorder\"/>. Raised after a successful\r\n/// drag-and-drop reorder of <see cref=\"TabsTrigger\"/> headers when the\r\n/// enclosing <see cref=\"TabsList\"/> has <c>Reorderable=\"true\"</c>.\r\n/// </summary>\r\n/// <param name=\"FromIndex\">Zero-based position of the moved trigger before the drop.</param>\r\n/// <param name=\"ToIndex\">Zero-based position the trigger was dropped onto.</param>\r\n/// <param name=\"MovedTabValue\">The <c>Value</c> of the trigger that was moved.</param>\r\n/// <remarks>\r\n/// The library does <b>not</b> mutate the underlying tab collection — the\r\n/// consumer's handler is expected to update its own data source and trigger a\r\n/// re-render so the new order takes effect.\r\n/// </remarks>\r\npublic sealed record TabsReorderEventArgs(int FromIndex, int ToIndex, string MovedTabValue);\r\n"
    },
    {
      "path": "UI/Tabs/TabsTrigger.razor",
      "content": "@namespace Lumeo\r\n@inject Lumeo.Services.Localization.ILumeoLocalizer L\r\n\r\n@{\r\n    // Register this trigger's position in the per-render ordered list used\r\n    // for drag-reorder. Order list is reset by TabsList at the start of each\r\n    // render; triggers append themselves here, so list order matches DOM order.\r\n    var reorderState = Context.Reorder;\r\n    var reorderActive = reorderState.Enabled && !Disabled;\r\n    if (reorderState.Enabled && !reorderState.Order.Contains(Value))\r\n    {\r\n        reorderState.Order.Add(Value);\r\n    }\r\n}\r\n\r\n<button type=\"button\"\r\n        id=\"@_tabId\"\r\n        role=\"tab\"\r\n        aria-selected=\"@IsActive.ToString().ToLower()\"\r\n        aria-disabled=\"@(Disabled ? \"true\" : null)\"\r\n        aria-controls=\"@_panelId\"\r\n        tabindex=\"@(Disabled ? -1 : IsActive ? 0 : -1)\"\r\n        class=\"@CssClass\"\r\n        draggable=\"@(reorderActive ? \"true\" : null)\"\r\n        data-sortable-item=\"@(reorderActive ? \"\" : null)\"\r\n        data-sortable-index=\"@(reorderActive ? reorderState.Order.IndexOf(Value).ToString() : null)\"\r\n        @ondragstart=\"HandleDragStart\"\r\n        @ondragover=\"HandleDragOver\"\r\n        @ondragover:preventDefault=\"@reorderActive\"\r\n        @ondrop=\"HandleDrop\"\r\n        @ondrop:preventDefault=\"@reorderActive\"\r\n        @ondragend=\"HandleDragEnd\"\r\n        @onclick=\"Activate\"\r\n        @onkeydown=\"HandleKeyDown\"\r\n        @onkeydown:preventDefault=\"@_preventKey\"\r\n        @attributes=\"AdditionalAttributes\">\r\n    @if (IconContent is not null)\r\n    {\r\n        <span class=\"inline-flex shrink-0\">@IconContent</span>\r\n    }\r\n    @ChildContent\r\n    @if (TrailingContent is not null)\r\n    {\r\n        <span class=\"inline-flex shrink-0\">@TrailingContent</span>\r\n    }\r\n    @if (IsClosable)\r\n    {\r\n        <span class=\"ml-1 inline-flex items-center justify-center rounded-sm cursor-pointer opacity-70 hover:opacity-100 transition-opacity h-11 w-11 -my-2 sm:h-4 sm:w-4 sm:my-0 focus-visible:outline-none focus-visible:ring-[1.5px] focus-visible:ring-ring focus-visible:ring-offset-1\"\r\n              @onclick=\"HandleClose\" @onclick:stopPropagation=\"true\"\r\n              role=\"button\" aria-label=\"@L[\"Tabs.CloseTab\"]\">\r\n            <Blazicon Svg=\"Lucide.X\" class=\"h-3 w-3\" />\r\n        </span>\r\n    }\r\n</button>\r\n\r\n@inject IComponentInteropService Interop\r\n@inject ThemeService Theme\r\n\r\n@code {\r\n    [CascadingParameter] public Tabs.TabsContext Context { get; set; } = default!;\r\n    [Parameter] public RenderFragment? ChildContent { get; set; }\r\n\r\n    /// <summary>Icon slot. Named <c>IconContent</c> to avoid shadowing the <c>&lt;Icon&gt;</c> component.</summary>\r\n    [Parameter] public RenderFragment? IconContent { get; set; }\r\n\r\n    [Parameter] public RenderFragment? TrailingContent { get; set; }\r\n    [Parameter, EditorRequired] public string Value { get; set; } = \"\";\r\n    [Parameter] public bool IsClosable { get; set; }\r\n    [Parameter] public bool Disabled { get; set; }\r\n    [Parameter] public EventCallback OnClose { get; set; }\r\n    [Parameter] public string? Class { get; set; }\r\n    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }\r\n\r\n    private readonly string _tabId = LumeoIds.New(\"tab\");\r\n    private readonly string _panelId = LumeoIds.New(\"tabpanel\");\r\n    private bool _preventKey;\r\n\r\n    private bool IsActive => Context.ActiveValue == Value;\r\n    private Tabs.TabsVariant CurrentVariant => Context.Variant;\r\n\r\n    protected override void OnInitialized()\r\n    {\r\n        Context.TabIds[Value] = (_tabId, _panelId);\r\n    }\r\n\r\n    private string CssClass\r\n    {\r\n        get\r\n        {\r\n            var disabledClasses = Disabled ? \"opacity-50 cursor-not-allowed pointer-events-none\" : \"\";\r\n            var variantClasses = CurrentVariant switch\r\n            {\r\n                Tabs.TabsVariant.Card => IsActive\r\n                    ? \"rounded-t-lg border border-b-0 border-border/40 bg-card text-foreground -mb-px\"\r\n                    : \"text-muted-foreground hover:text-foreground\",\r\n                Tabs.TabsVariant.Pill => IsActive\r\n                    ? \"rounded-full bg-primary text-primary-foreground\"\r\n                    : \"text-muted-foreground hover:bg-muted/50 rounded-full\",\r\n                _ => IsActive\r\n                    ? \"bg-background text-foreground shadow\"\r\n                    : \"\"\r\n            };\r\n\r\n            var reorderState = Context.Reorder;\r\n            var reorderActive = reorderState.Enabled && !Disabled;\r\n            var isDragging = reorderActive && reorderState.DraggingValue == Value;\r\n            var isDragOver = reorderActive && reorderState.DragOverValue == Value && reorderState.DraggingValue != Value;\r\n\r\n            // Smooth transform transitions animate any sibling shifts after drop.\r\n            // Only enable touch-friendly grab cursor when this trigger can be dragged.\r\n            var reorderClasses = reorderActive\r\n                ? \"transition-transform duration-150 ease-out cursor-grab active:cursor-grabbing select-none\"\r\n                : \"\";\r\n            if (isDragging) reorderClasses += \" opacity-50\";\r\n            if (isDragOver) reorderClasses += Context.Orientation == Lumeo.Orientation.Vertical\r\n                ? \" ring-2 ring-primary ring-offset-0\"\r\n                : \" ring-2 ring-primary ring-offset-0\";\r\n\r\n            return $\"inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md px-3 py-1.5 text-sm font-medium cursor-pointer transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 disabled:cursor-not-allowed {variantClasses} {disabledClasses} {reorderClasses} {Class}\".Trim();\r\n        }\r\n    }\r\n\r\n    private async Task Activate()\r\n    {\r\n        if (Disabled) return;\r\n        // Don't activate when finishing a drag (a click event fires after dragend).\r\n        if (Context.Reorder.Enabled && Context.Reorder.DraggingValue == Value) return;\r\n        await Context.SetValue.InvokeAsync(Value);\r\n    }\r\n\r\n    private void HandleDragStart()\r\n    {\r\n        if (Context.Reorder.Enabled && !Disabled)\r\n            Context.Reorder.OnDragStart?.Invoke(Value);\r\n    }\r\n\r\n    private void HandleDragOver()\r\n    {\r\n        // Disabled triggers are not valid drop targets — skip both the visual\r\n        // hint and the over-tracking so a dragged item passes through.\r\n        if (Context.Reorder.Enabled && !Disabled && Context.Reorder.DraggingValue is not null)\r\n            Context.Reorder.OnDragOver?.Invoke(Value);\r\n    }\r\n\r\n    private async Task HandleDrop()\r\n    {\r\n        if (Context.Reorder.Enabled && !Disabled && Context.Reorder.OnDrop is not null)\r\n            await Context.Reorder.OnDrop.Invoke(Value);\r\n    }\r\n\r\n    private void HandleDragEnd()\r\n    {\r\n        // Always fire so the source clears its state even if dropped outside.\r\n        if (Context.Reorder.Enabled)\r\n            Context.Reorder.OnDragEnd?.Invoke();\r\n    }\r\n\r\n    private async Task HandleClose()\r\n    {\r\n        await OnClose.InvokeAsync();\r\n    }\r\n\r\n    private async Task HandleKeyDown(KeyboardEventArgs e)\r\n    {\r\n        if (Disabled) return;\r\n\r\n        var isHorizontal = Context.Orientation == Lumeo.Orientation.Horizontal;\r\n        var isRtl = Theme.CurrentDirection == LayoutDirection.Rtl;\r\n        var keys = Context.TabIds.Keys.ToList();\r\n        var idx = keys.IndexOf(Value);\r\n\r\n        // In RTL, ArrowLeft/Right are swapped for horizontal tabs so they follow visual flow.\r\n        var (hBack, hForward) = (isHorizontal && isRtl) ? (\"ArrowRight\", \"ArrowLeft\") : (\"ArrowLeft\", \"ArrowRight\");\r\n\r\n        string? nextValue = null;\r\n        if (isHorizontal)\r\n        {\r\n            if (e.Key == hForward) nextValue = FindNextEnabledTab(keys, idx, 1);\r\n            else if (e.Key == hBack) nextValue = FindNextEnabledTab(keys, idx, -1);\r\n        }\r\n        else\r\n        {\r\n            if (e.Key == \"ArrowDown\") nextValue = FindNextEnabledTab(keys, idx, 1);\r\n            else if (e.Key == \"ArrowUp\") nextValue = FindNextEnabledTab(keys, idx, -1);\r\n        }\r\n        if (e.Key == \"Home\") nextValue = keys.Count > 0 ? keys[0] : null;\r\n        else if (e.Key == \"End\") nextValue = keys.Count > 0 ? keys[^1] : null;\r\n\r\n        _preventKey = e.Key is \"ArrowRight\" or \"ArrowLeft\" or \"ArrowDown\" or \"ArrowUp\" or \"Home\" or \"End\";\r\n\r\n        if (nextValue is not null && Context.TabIds.TryGetValue(nextValue, out var ids))\r\n        {\r\n            await Context.SetValue.InvokeAsync(nextValue);\r\n            await Interop.FocusElement(ids.TabId);\r\n        }\r\n    }\r\n\r\n    private string? FindNextEnabledTab(List<string> keys, int currentIdx, int direction)\r\n    {\r\n        if (keys.Count == 0) return null;\r\n        var nextIdx = currentIdx + direction;\r\n        if (nextIdx < 0) nextIdx = keys.Count - 1;\r\n        else if (nextIdx >= keys.Count) nextIdx = 0;\r\n        return keys[nextIdx];\r\n    }\r\n}\r\n"
    }
  ],
  "slots": [
    {
      "name": "ChildContent",
      "owner": "Tabs",
      "type": "RenderFragment?",
      "description": null
    },
    {
      "name": "ChildContent",
      "owner": "TabsContent",
      "type": "RenderFragment?",
      "description": null
    },
    {
      "name": "ChildContent",
      "owner": "TabsList",
      "type": "RenderFragment?",
      "description": null
    },
    {
      "name": "TrailingContent",
      "owner": "TabsList",
      "type": "RenderFragment?",
      "description": null
    },
    {
      "name": "ChildContent",
      "owner": "TabsTrigger",
      "type": "RenderFragment?",
      "description": null
    },
    {
      "name": "IconContent",
      "owner": "TabsTrigger",
      "type": "RenderFragment?",
      "description": "Icon slot. Named IconContent to avoid shadowing the &lt;Icon&gt; component."
    },
    {
      "name": "TrailingContent",
      "owner": "TabsTrigger",
      "type": "RenderFragment?",
      "description": null
    }
  ],
  "serviceDependencies": [
    {
      "service": "IComponentInteropService",
      "namespace": "Lumeo.Services"
    },
    {
      "service": "ILumeoLocalizer",
      "namespace": "Lumeo.Services.Localization"
    },
    {
      "service": "IComponentInteropService",
      "namespace": ""
    },
    {
      "service": "ThemeService",
      "namespace": ""
    }
  ],
  "cascadingDependencies": [
    {
      "name": "Context",
      "type": "Tabs.TabsContext",
      "required": true
    }
  ],
  "relatedComponents": [
    {
      "name": "TabsContent",
      "reason": "sub-component"
    },
    {
      "name": "TabsList",
      "reason": "sub-component"
    },
    {
      "name": "TabsTrigger",
      "reason": "sub-component"
    }
  ],
  "keyboardInteractions": [
    {
      "key": "ArrowDown",
      "action": "HandleKeyDown handles ArrowDown in TabsTrigger.razor"
    },
    {
      "key": "ArrowUp",
      "action": "HandleKeyDown handles ArrowUp in TabsTrigger.razor"
    },
    {
      "key": "Home",
      "action": "HandleKeyDown handles Home in TabsTrigger.razor"
    },
    {
      "key": "End",
      "action": "HandleKeyDown handles End in TabsTrigger.razor"
    }
  ],
  "tests": [
    "tests/Lumeo.Tests/Components/Tabs/TabsSwipeReregistrationTests.cs",
    "tests/Lumeo.Tests/Components/Tabs/TabsSwipeTests.cs"
  ],
  "mdSummary": "# Tabs\r\n\r\nTabbed content with keyboard nav and animated active indicator.\r\n\r\n**Package:** `Lumeo` · **Category:** Navigation\r\n**Docs:** https://lumeo.nativ.sh/components/tabs\r\n\r\n## Parameters\r\n| Owner | Name | Type | Default | Description |\r\n|---|---|---|---|---|\r\n| Tabs | ChildContent | `RenderFragment?` | `` |  |\r\n| Tabs | ActiveValue | `string` | `\"\"` |  |\r\n| Tabs | ActiveValueChanged | `EventCallback<string>` | `` |  |\r\n| Tabs | Orientation | `Lumeo.Orientation` | `Lumeo.Orientation.Horizontal` |  |\r\n| Tabs | Variant | `TabsVariant` | `TabsVariant.Default` |  |\r\n| Tabs | AnimatedIndicator | `bool` | `` | When true, renders a sliding underline that animates between active triggers inside each TabsList. Default is false (no underline, original behaviour). |\r\n| Tabs | RenderMode | `TabsRenderMode` | `TabsRenderMode.Active` | Controls when each TabsContent panel is rendered into the DOM and whether its state survives a tab switch: Active (default) — only the active panel is in the DOM; switching away unmounts it (form inputs, scroll position and any component state are lost). Lightest footprint. Lazy — a panel is rendered the first time its tab becomes active, then kept in the DOM (hidden) so its state is preserved on subsequent switches. Like DevExpress OnDemand. Eager — every panel is rendered immediately; inactive ones are hidden. State always preserved, but the heaviest initial render. Like DevExpress Always. A single TabsContent can opt into eager rendering regardless of this setting via ForceMount=\"true\". |\r\n| Tabs | SwipeEnabled | `bool` | `` | When true, a horizontal pointer drag (&gt;50 px horizontal, &lt;30 px vertical) on any TabsContent panel switches to the next or previous tab. Default is false (opt-in). |\r\n| Tabs | SwipeWrap | `bool` | `` | When true, swiping past the last tab wraps around to the first (and vice-versa). Default is false (no wrap-around at boundaries). |\r\n| Tabs | Class | `string?` | `` |  |\r\n| Tabs | AdditionalAttributes | `Dictionary<string, object>?` | `` |  |\r\n| TabsContent | Context | `Tabs.TabsContext` | `default!` |  |\r\n| TabsContent | ChildContent | `RenderFragment?` | `` |  |\r\n| TabsContent | Value | `string` | `\"\"` |  |\r\n| TabsContent | ForceMount | `bool` | `` | When true, this panel is always rendered into the DOM (hidden when inactive), regardless of the parent RenderMode. Use for panels whose state must survive every tab switch — e.g. a half-filled form — while leaving the rest of the tab set on the lighter default mode. |\r\n| TabsContent | Class | `string?` | `` |  |\r\n| TabsContent | AdditionalAttributes | `Dictionary<string, object>?` | `` |  |\r\n| TabsList | Context | `Tabs.TabsContext?` | `` |  |\r\n| TabsList | ChildContent | `RenderFragment?` | `` |  |\r\n| TabsList | TrailingContent | `RenderFragment?` | `` |  |\r\n| TabsList | Scrollable | `bool` | `` |  |\r\n| TabsList | Reorderable | `bool` | `` | When true, each enabled TabsTrigger becomes draggable (HTML5 drag-and-drop on desktop, touch drag on mobile). On a successful reorder the library raises OnReorder with the from/to indices and moved value — the library does NOT mutate the order. The consumer's handler is responsible for updating the underlying collection and re-rendering. |\r\n| TabsList | OnReorder | `EventCallback<TabsReorderEventArgs>` | `` | Fires when the user drops a dragged TabsTrigger on a new position. Only meaningful when Reorderable is true. |\r\n| TabsList | SavedLayout | `TabsLayout?` | `` | Optional previously-persisted tab ordering. The library does not mutate the consumer's backing collection on its own — pass this to your parent component on init and apply Order to your collection before rendering. Mirrors DataGrid's SavedLayout parameter and pairs with OnLayoutChanged to round-trip the order through your storage of choice. |\r\n| TabsList | OnLayoutChanged | `EventCallback<TabsLayout>` | `` | Fires immediately after each successful reorder with the post-drop tab order, so the consumer can persist it (e.g. to local storage). The snapshot reflects the order the consumer's collection will have once it applies the corresponding OnReorder. Only meaningful when Reorderable is true. Pairs with SavedLayout to round-trip the order through storage. |\r\n| TabsList | Class | `string?` | `` |  |\r\n| TabsList | AdditionalAttributes | `Dictionary<string, object>?` | `` |  |\r\n| TabsTrigger | Context | `Tabs.TabsContext` | `default!` |  |\r\n| TabsTrigger | ChildContent | `RenderFragment?` | `` |  |\r\n| TabsTrigger | IconContent | `RenderFragment?` | `` | Icon slot. Named IconContent to avoid shadowing the &lt;Icon&gt; component. |\r\n| TabsTrigger | TrailingContent | `RenderFragment?` | `` |  |\r\n| TabsTrigger | Value | `string` | `\"\"` |  |\r\n| TabsTrigger | IsClosable | `bool` | `` |  |\r\n| TabsTrigger | Disabled | `bool` | `` |  |\r\n| TabsTrigger | OnClose | `EventCallback` | `` |  |\r\n| TabsTrigger | Class | `string?` | `` |  |\r\n| TabsTrigger | AdditionalAttributes | `Dictionary<string, object>?` | `` |  |\r\n\r\n## Slots\r\n- `ChildContent` (Tabs) — RenderFragment slot\r\n- `ChildContent` (TabsContent) — RenderFragment slot\r\n- `ChildContent` (TabsList) — RenderFragment slot\r\n- `TrailingContent` (TabsList) — RenderFragment slot\r\n- `ChildContent` (TabsTrigger) — RenderFragment slot\r\n- `IconContent` (TabsTrigger) — Icon slot. Named IconContent to avoid shadowing the &lt;Icon&gt; component.\r\n- `TrailingContent` (TabsTrigger) — RenderFragment slot\r\n\r\n## Events\r\n- `ActiveValueChanged` (`EventCallback<string>`)\r\n\r\n## Sub-components\r\n- `TabsContent`, `TabsList`, `TabsTrigger`\r\n\r\n## Cascading values it expects\r\n- `Context` (`Tabs.TabsContext`)\r\n\r\n## Services it injects\r\n- `IComponentInteropService` (Lumeo.Services)\r\n- `ILumeoLocalizer` (Lumeo.Services.Localization)\r\n- `IComponentInteropService` ()\r\n- `ThemeService` ()\r\n\r\n## Keyboard\r\n- `ArrowDown` — HandleKeyDown handles ArrowDown in TabsTrigger.razor\r\n- `ArrowUp` — HandleKeyDown handles ArrowUp in TabsTrigger.razor\r\n- `Home` — HandleKeyDown handles Home in TabsTrigger.razor\r\n- `End` — HandleKeyDown handles End in TabsTrigger.razor\r\n\r\n## Example\r\n```razor\r\n<Tabs />\r\n```\r\n\r\n## CSS variables\r\n`--color-background`, `--color-border`, `--color-card`, `--color-foreground`, `--color-muted`, `--color-muted-foreground`, `--color-primary`, `--color-primary-foreground`, `--color-ring`, `--radius`\r\n\r\n## Files\r\n- `UI/Tabs/Tabs.razor`\r\n- `UI/Tabs/TabsContent.razor`\r\n- `UI/Tabs/TabsLayout.cs`\r\n- `UI/Tabs/TabsList.razor`\r\n- `UI/Tabs/TabsReorderEventArgs.cs`\r\n- `UI/Tabs/TabsTrigger.razor`\r\n"
}