@page "/components/context-menu"

<PageTitle>Context Menu — Lumeo</PageTitle>

<PageHeader Title="Context Menu" Description="Displays a menu triggered by right-clicking an element." />

<InstallUsage Component="ContextMenu" />

<div class="mt-10 space-y-3">
    <Heading Id="when-to-use" Level="2" Class="scroll-mt-20">When to Use</Heading>
    <ul class="list-disc list-inside space-y-1 text-muted-foreground text-sm">
        <li>Right-click actions on elements like files, cards, or table rows</li>
        <li>Custom context menus replacing the browser default for specific areas</li>
        <li>Element-specific operations such as copy, edit, delete, or move</li>
    </ul>
</div>

<Stack Gap="8" Class="mt-8">
    <ComponentDemo Title="Default" Code="@_defaultCode">
        <ContextMenu>
            <ContextMenuTrigger>
                <Flex Class="h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm text-muted-foreground">
                    Right click here
                </Flex>
            </ContextMenuTrigger>
            <ContextMenuContent>
                <ContextMenuItem>Back</ContextMenuItem>
                <ContextMenuItem>Forward</ContextMenuItem>
                <ContextMenuItem>Reload</ContextMenuItem>
                <ContextMenuSeparator />
                <ContextMenuItem>Save As...</ContextMenuItem>
                <ContextMenuItem>Print</ContextMenuItem>
            </ContextMenuContent>
        </ContextMenu>
    </ComponentDemo>

    <ComponentDemo Title="With Labels and Groups" Code="@_groupCode">
        <ContextMenu>
            <ContextMenuTrigger>
                <Flex Class="h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm text-muted-foreground">
                    Right click here
                </Flex>
            </ContextMenuTrigger>
            <ContextMenuContent>
                <ContextMenuLabel>Edit</ContextMenuLabel>
                <ContextMenuGroup>
                    <ContextMenuItem>Cut</ContextMenuItem>
                    <ContextMenuItem>Copy</ContextMenuItem>
                    <ContextMenuItem>Paste</ContextMenuItem>
                </ContextMenuGroup>
                <ContextMenuSeparator />
                <ContextMenuLabel>View</ContextMenuLabel>
                <ContextMenuGroup>
                    <ContextMenuItem>Zoom In</ContextMenuItem>
                    <ContextMenuItem>Zoom Out</ContextMenuItem>
                    <ContextMenuItem>Reset Zoom</ContextMenuItem>
                </ContextMenuGroup>
                <ContextMenuSeparator />
                <ContextMenuItem Disabled="true">Delete</ContextMenuItem>
            </ContextMenuContent>
        </ContextMenu>
    </ComponentDemo>

    <ComponentDemo Title="Checkbox and Radio Items">
        <ContextMenu>
            <ContextMenuTrigger>
                <Flex Class="h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm text-muted-foreground">
                    Right click here
                </Flex>
            </ContextMenuTrigger>
            <ContextMenuContent>
                <ContextMenuLabel>Appearance</ContextMenuLabel>
                <ContextMenuCheckboxItem @bind-Checked="_showBookmarks">Show Bookmarks</ContextMenuCheckboxItem>
                <ContextMenuCheckboxItem @bind-Checked="_showFullUrls">Show Full URLs</ContextMenuCheckboxItem>
                <ContextMenuSeparator />
                <ContextMenuLabel>Panel Position</ContextMenuLabel>
                <ContextMenuRadioGroup @bind-Value="_panelPosition">
                    <ContextMenuRadioItem Value="left">Left</ContextMenuRadioItem>
                    <ContextMenuRadioItem Value="right">Right</ContextMenuRadioItem>
                    <ContextMenuRadioItem Value="bottom">Bottom</ContextMenuRadioItem>
                </ContextMenuRadioGroup>
            </ContextMenuContent>
        </ContextMenu>
    </ComponentDemo>

    <ComponentDemo Title="File Actions" Code="@_fileCode">
        <ContextMenu>
            <ContextMenuTrigger>
                <Flex Align="center" Justify="center" Class="h-[150px] w-[300px] rounded-md border border-dashed">
                    <Stack Gap="0" Class="text-center">
                        <Lumeo.Text Size="sm" Weight="medium">document.pdf</Lumeo.Text>
                        <Lumeo.Text Size="xs" Color="muted" Class="mt-1">Right click for options</Lumeo.Text>
                    </Stack>
                </Flex>
            </ContextMenuTrigger>
            <ContextMenuContent>
                <ContextMenuItem>
                    <DynamicIcon Name="Eye" Class="mr-2 h-4 w-4" />
                    Open
                </ContextMenuItem>
                <ContextMenuItem>
                    <DynamicIcon Name="Copy" Class="mr-2 h-4 w-4" />
                    Duplicate
                </ContextMenuItem>
                <ContextMenuSeparator />
                <ContextMenuItem>
                    <DynamicIcon Name="ArrowRight" Class="mr-2 h-4 w-4" />
                    Move to...
                </ContextMenuItem>
                <ContextMenuSeparator />
                <ContextMenuItem>
                    <DynamicIcon Name="Star" Class="mr-2 h-4 w-4" />
                    Add to Favorites
                </ContextMenuItem>
                <ContextMenuSeparator />
                <ContextMenuItem>
                    <DynamicIcon Name="Trash" Class="mr-2 h-4 w-4" />
                    Delete
                </ContextMenuItem>
            </ContextMenuContent>
        </ContextMenu>
    </ComponentDemo>

    <ComponentDemo Title="Destructive, Inset & Shortcut Items" Code="@_destructiveCode" Description="ContextMenuShortcut right-aligns a keyboard hint; Variant=Destructive tints a dangerous action; Inset aligns label-only items beside icon items.">
        <ContextMenu>
            <ContextMenuTrigger>
                <Flex Class="h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm text-muted-foreground">
                    Right click here
                </Flex>
            </ContextMenuTrigger>
            <ContextMenuContent>
                <ContextMenuItem>
                    <DynamicIcon Name="Pencil" Class="mr-2 h-4 w-4" /> Rename <ContextMenuShortcut>F2</ContextMenuShortcut>
                </ContextMenuItem>
                <ContextMenuItem Inset="true">Duplicate <ContextMenuShortcut>Ctrl+D</ContextMenuShortcut></ContextMenuItem>
                <ContextMenuSeparator />
                <ContextMenuItem Variant="MenuItemVariant.Destructive">
                    <DynamicIcon Name="Trash" Class="mr-2 h-4 w-4" /> Delete <ContextMenuShortcut>Ctrl+Del</ContextMenuShortcut>
                </ContextMenuItem>
            </ContextMenuContent>
        </ContextMenu>
    </ComponentDemo>
</Stack>

<Alert Variant="Alert.AlertVariant.Info" ShowIcon="true" Class="mt-6">
    <strong>Mobile (rc.49).</strong> Context menus now open on a long-press (500 ms by default) on touch devices; use <code class="rounded bg-muted px-1.5 py-0.5 text-xs font-mono">LongPressMs</code> to adjust the delay and <code class="rounded bg-muted px-1.5 py-0.5 text-xs font-mono">MoveTolerancePx</code> (default 10) to set how far a finger may drift before the press is cancelled.
</Alert>

<div class="mt-12 space-y-4">
    <Heading Id="submenus" Level="2" Class="scroll-mt-20">Submenus</Heading>
    <Lumeo.Text Size="base" Color="muted" Leading="relaxed">
        Nest menus with the <code class="rounded bg-muted px-1.5 py-0.5 text-xs font-mono text-foreground">ContextMenuSub</code> /
        <code class="rounded bg-muted px-1.5 py-0.5 text-xs font-mono text-foreground">ContextMenuSubTrigger</code> /
        <code class="rounded bg-muted px-1.5 py-0.5 text-xs font-mono text-foreground">ContextMenuSubContent</code>
        triplet. Submenus open on hover (with intent delay), and nesting is recursive so you can build deep
        right-click trees.
    </Lumeo.Text>

    <ComponentDemo Title="Submenus" Code="@_submenusCode">
        <ContextMenu>
            <ContextMenuTrigger>
                <div class="rounded border border-dashed p-10 text-center text-muted-foreground">Right-click me</div>
            </ContextMenuTrigger>
            <ContextMenuContent>
                <ContextMenuItem>Copy</ContextMenuItem>

                <ContextMenuSub>
                    <ContextMenuSubTrigger>Share</ContextMenuSubTrigger>
                    <ContextMenuSubContent>
                        <ContextMenuItem>Email</ContextMenuItem>
                        <ContextMenuItem>Link</ContextMenuItem>

                        <ContextMenuSub>
                            <ContextMenuSubTrigger>Export as…</ContextMenuSubTrigger>
                            <ContextMenuSubContent>
                                <ContextMenuItem>PDF</ContextMenuItem>
                                <ContextMenuItem>PNG</ContextMenuItem>
                            </ContextMenuSubContent>
                        </ContextMenuSub>
                    </ContextMenuSubContent>
                </ContextMenuSub>

                <ContextMenuSeparator />
                <ContextMenuItem>Delete</ContextMenuItem>
            </ContextMenuContent>
        </ContextMenu>
    </ComponentDemo>

    <Lumeo.Text Size="base" Color="muted" Leading="relaxed">
        Keyboard:
    </Lumeo.Text>
    <ul class="space-y-1 text-sm text-muted-foreground list-disc list-inside">
        <li><Kbd>ArrowRight</Kbd> on a <code class="rounded bg-muted px-1.5 py-0.5 text-xs font-mono text-foreground">SubTrigger</code> opens the submenu and focuses its first item.</li>
        <li><Kbd>ArrowLeft</Kbd> closes the current submenu and returns focus to the parent trigger.</li>
        <li><Kbd>Escape</Kbd> closes the entire context menu, not just the current submenu.</li>
        <li><strong>Type-ahead:</strong> with the menu open, start typing a label and focus jumps to the first matching item; keep typing to refine, or pause to reset the search buffer.</li>
    </ul>
</div>

<div class="mt-16">
    <Heading Id="api-reference" Level="2" Class="scroll-mt-20">API Reference</Heading>

    <Heading Id="contextmenu" Level="3" Class="mt-8 scroll-mt-20">ContextMenu</Heading>
    <PropsTable Slug="context-menu" />

    <Heading Id="contextmenutrigger" Level="3" Class="mt-8 scroll-mt-20">ContextMenuTrigger</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuTrigger" />

    <Heading Id="contextmenucontent" Level="3" Class="mt-8 scroll-mt-20">ContextMenuContent</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuContent" />

    <Heading Id="contextmenuitem" Level="3" Class="mt-8 scroll-mt-20">ContextMenuItem</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuItem" />

    <Heading Id="contextmenushortcut" Level="3" Class="mt-8 scroll-mt-20">ContextMenuShortcut</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuShortcut" />

    <Heading Id="contextmenucheckboxitem" Level="3" Class="mt-8 scroll-mt-20">ContextMenuCheckboxItem</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuCheckboxItem" />

    <Heading Id="contextmenuradiogroup" Level="3" Class="mt-8 scroll-mt-20">ContextMenuRadioGroup</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuRadioGroup" />

    <Heading Id="contextmenuradioitem" Level="3" Class="mt-8 scroll-mt-20">ContextMenuRadioItem</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuRadioItem" />

    <Heading Id="contextmenulabel" Level="3" Class="mt-8 scroll-mt-20">ContextMenuLabel</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuLabel" />

    <Heading Id="contextmenuseparator" Level="3" Class="mt-8 scroll-mt-20">ContextMenuSeparator</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuSeparator" />

    <Heading Id="contextmenugroup" Level="3" Class="mt-8 scroll-mt-20">ContextMenuGroup</Heading>
    <PropsTable Slug="context-menu" SubComponent="ContextMenuGroup" />
</div>

<div class="mt-12">
    <Heading Id="related" Level="2" Class="scroll-mt-20">Related Components</Heading>
    <ul class="mt-4 space-y-2 text-sm">
        <li><a href="/components/dropdown-menu" class="text-primary hover:underline">DropdownMenu</a> — For action menus triggered by a click on a button or icon</li>
        <li><a href="/components/menubar" class="text-primary hover:underline">Menubar</a> — For persistent application menu bars with multiple dropdown menus</li>
        <li><a href="/components/popover" class="text-primary hover:underline">Popover</a> — For displaying arbitrary content in a floating panel on click</li>
    </ul>
</div>

@code {
    private bool _showBookmarks = true;
    private bool _showFullUrls;
    private string _panelPosition = "right";

    private string _destructiveCode = @"<ContextMenu>
    <ContextMenuTrigger>
        <div class=""flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm"">
            Right click here
        </div>
    </ContextMenuTrigger>
    <ContextMenuContent>
        <ContextMenuItem>
            <SvgGlyph Svg=""@(Lucide.Pencil)"" class=""mr-2 h-4 w-4"" /> Rename <ContextMenuShortcut>F2</ContextMenuShortcut>
        </ContextMenuItem>
        <ContextMenuItem Inset=""true"">Duplicate <ContextMenuShortcut>Ctrl+D</ContextMenuShortcut></ContextMenuItem>
        <ContextMenuSeparator />
        <ContextMenuItem Variant=""MenuItemVariant.Destructive"">
            <SvgGlyph Svg=""@(Lucide.Trash)"" class=""mr-2 h-4 w-4"" /> Delete <ContextMenuShortcut>Ctrl+Del</ContextMenuShortcut>
        </ContextMenuItem>
    </ContextMenuContent>
</ContextMenu>";

    private string _submenusCode = @"<ContextMenu>
    <ContextMenuTrigger>
        <div class=""rounded border border-dashed p-10 text-center"">Right-click</div>
    </ContextMenuTrigger>
    <ContextMenuContent>
        <ContextMenuItem>Copy</ContextMenuItem>

        <ContextMenuSub>
            <ContextMenuSubTrigger>Share</ContextMenuSubTrigger>
            <ContextMenuSubContent>
                <ContextMenuItem>Email</ContextMenuItem>
                <ContextMenuItem>Link</ContextMenuItem>

                <ContextMenuSub>
                    <ContextMenuSubTrigger>Export as…</ContextMenuSubTrigger>
                    <ContextMenuSubContent>
                        <ContextMenuItem>PDF</ContextMenuItem>
                        <ContextMenuItem>PNG</ContextMenuItem>
                    </ContextMenuSubContent>
                </ContextMenuSub>
            </ContextMenuSubContent>
        </ContextMenuSub>

        <ContextMenuSeparator />
        <ContextMenuItem>Delete</ContextMenuItem>
    </ContextMenuContent>
</ContextMenu>";

    private string _defaultCode = @"<ContextMenu>
    <ContextMenuTrigger>
        <div class=""flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm"">
            Right click here
        </div>
    </ContextMenuTrigger>
    <ContextMenuContent>
        <ContextMenuItem>Back</ContextMenuItem>
        <ContextMenuItem>Forward</ContextMenuItem>
        <ContextMenuItem>Reload</ContextMenuItem>
        <ContextMenuSeparator />
        <ContextMenuItem>Save As...</ContextMenuItem>
        <ContextMenuItem>Print</ContextMenuItem>
    </ContextMenuContent>
</ContextMenu>";

    private string _groupCode = @"<ContextMenu>
    <ContextMenuTrigger>
        <div>Right click here</div>
    </ContextMenuTrigger>
    <ContextMenuContent>
        <ContextMenuLabel>Edit</ContextMenuLabel>
        <ContextMenuGroup>
            <ContextMenuItem>Cut</ContextMenuItem>
            <ContextMenuItem>Copy</ContextMenuItem>
            <ContextMenuItem>Paste</ContextMenuItem>
        </ContextMenuGroup>
        <ContextMenuSeparator />
        <ContextMenuLabel>View</ContextMenuLabel>
        <ContextMenuGroup>
            <ContextMenuItem>Zoom In</ContextMenuItem>
            <ContextMenuItem>Zoom Out</ContextMenuItem>
        </ContextMenuGroup>
        <ContextMenuSeparator />
        <ContextMenuItem Disabled=""true"">Delete</ContextMenuItem>
    </ContextMenuContent>
</ContextMenu>";

    private string _fileCode = @"<ContextMenu>
    <ContextMenuTrigger>
        <div class=""flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed"">
            <div class=""text-center"">
                <div class=""text-sm font-medium"">document.pdf</div>
                <div class=""text-xs text-muted-foreground mt-1"">Right click for options</div>
            </div>
        </div>
    </ContextMenuTrigger>
    <ContextMenuContent>
        <ContextMenuItem>Open</ContextMenuItem>
        <ContextMenuItem>Duplicate</ContextMenuItem>
        <ContextMenuSeparator />
        <ContextMenuItem>Move to...</ContextMenuItem>
        <ContextMenuSeparator />
        <ContextMenuItem>Add to Favorites</ContextMenuItem>
        <ContextMenuSeparator />
        <ContextMenuItem>Delete</ContextMenuItem>
    </ContextMenuContent>
</ContextMenu>";
}
